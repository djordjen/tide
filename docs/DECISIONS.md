# TIDE Decision Log

This document records the current design direction. Dates indicate when a
decision was first recorded, not when implementation was completed.

## Accepted decisions

| Date | Decision | Rationale |
|---|---|---|
| 2026-07-14 | The project name is **TIDE: Terminal Integrated Data Environment**. | Memorable, terminal-oriented, and not tied to a legacy product name. |
| 2026-07-14 | TIDE is a business-application runtime; Textual is its first UI adapter. | Domain behavior must remain independent from presentation technology. |
| 2026-07-14 | Python, Textual, SQLAlchemy, Alembic, Pydantic, SQLite, PostgreSQL, FastAPI, and pytest form the initial ecosystem. | They are mainstream, productive, and replaceable behind clear adapter contracts. |
| 2026-07-14 | YAML is the preferred human authoring format; JSON may be accepted and exported. | Compact YAML is readable and supports comments; the compiler remains format-independent. |
| 2026-07-14 | Models are split by entity and concern rather than stored in one large file. | Smaller files are easier to review, merge, and explain. |
| 2026-07-14 | All source formats compile into one typed, immutable `ApplicationModel`. | Every adapter must share identical metadata semantics. |
| 2026-07-14 | Co-located applications live under `applications/<name>/`, separate from `src/tide/`. | The framework/application ownership boundary should be obvious, while still allowing a web2py-style multi-application distribution. |
| 2026-07-14 | "Model compiler" means semantic metadata compilation, not Python bytecode or native executable compilation. | Validation, resolution, and normalization are required in development and at production startup; deployable Python remains ordinary source/package content. |
| 2026-07-14 | Runtime metadata precedes source generation. | It avoids regeneration conflicts while contracts are evolving. |
| 2026-07-14 | Generated views work without a designer; customizations are deterministic overlays. | Immediate productivity and safe model evolution are more important than early visual tooling. |
| 2026-07-14 | Application services, security, and `RecordSession` sit between every adapter and SQLAlchemy. | TUI, REST, MCP, reports, and web must not duplicate or bypass business rules. |
| 2026-07-14 | REST exposure is explicit through `expose.rest`; FastAPI supplies the initial adapter. | Exposure is security-sensitive and should generate OpenAPI without generated source files. |
| 2026-07-14 | TIDE has separate developer and runtime MCP use cases. | Project inspection and runtime data access have different risk and permission profiles. |
| 2026-07-14 | Security covers entity, row, field, action, report, navigation, and export permissions. | Hiding a control is not sufficient authorization. |
| 2026-07-14 | Protected fields use an internal sentinel, rendered as localized protected content in forms. | A display placeholder must never become a stored value or break typed fields. |
| 2026-07-14 | Cross-file and cross-module relationships are resolved through qualified entity names. | Modules need collision-free references and a global relationship graph. |
| 2026-07-14 | Computed fields, filters, validation, action conditions, and conditional presentation share a safe typed expression system. | One criteria model can be validated and translated consistently without `eval`. |
| 2026-07-14 | TIDE Studio edits metadata through a headless designer service. | Textual, web, and AI designer clients should share validation, undo/redo, and save behavior. |
| 2026-07-14 | Reports use a declarative banded model and secured application services. | This preserves Clarion-style reporting without creating a data-access back door. |
| 2026-07-14 | Metadata source schema `0.1` is explicit and separate from the application version. | Parsing changes cannot silently reinterpret an existing project. |
| 2026-07-14 | The v0.1 YAML reader uses safe construction, strict true/false booleans, duplicate-key rejection, and no YAML merge keys. | Ambiguous scalars and hidden duplicate properties are unsafe authoring behavior. |
| 2026-07-14 | The initial expression parser uses a validated, allow-listed Python AST subset but never `eval`. | It supplies a small familiar grammar while rejecting method calls, arbitrary nodes, and unknown paths. |
| 2026-07-14 | The first SQLAlchemy persistence adapter will be synchronous. | A single transaction model simplifies `RecordSession`; async action handlers remain possible and the adapter boundary permits revisiting this later. |
| 2026-07-14 | Basic optimistic version checking precedes every remote mutation. | REST and MCP writes must not ship with silent lost-update behavior; richer merge UX can follow later. |
| 2026-07-14 | Schema v0.1 is single-tenant per deployment. | Multi-user support is required, but tenancy needs explicit identity, uniqueness, cache, migration, and policy semantics rather than an optional context field. |
| 2026-07-14 | TIDE is distributed under the MIT License. | The project should be freely usable, modifiable, redistributable, sublicensable, and commercially usable with only preservation of the notice and warranty disclaimer. |
| 2026-07-14 | View resolution records leaf-level property provenance. | Defaults, presets, entity settings, base views, and overlays are only maintainable when the final value can identify its source. |
| 2026-07-14 | Compiler handler checks are static and do not import application modules. | Validation must confirm project-local functions without executing untrusted project code. |
| 2026-07-14 | The first application-service adapter is an in-memory repository. | Security, sessions, validation, actions, and concurrency can stabilize before SQLAlchemy mapping concerns are introduced. |
| 2026-07-14 | Persistence owns concurrency-token increments; domain actions own state transitions. | Central version management makes retries and stale-write handling consistent across all actions and adapters. |
| 2026-07-14 | Idempotency replay reauthorizes and reprojects its target. | Cached transport output could leak fields after permission changes; replay must use current authorization. |
| 2026-07-14 | Decimal fields use deterministic decimal arithmetic end to end: fractional literals preserve their source tokens, evaluation uses a private 38-digit round-half-even context, division and averages return `Decimal`, and record services normalize decimal inputs at the service boundary. | Business quantities such as money must not accumulate binary floating-point error or depend on process-global decimal settings. |
| 2026-07-14 | Field values are validated against their declared type at commit; values that cannot represent the type fail validation instead of being stored. | An invoicing runtime must not silently store a string in a date field; the SQLAlchemy adapter would otherwise disagree with the in-memory contract. |
| 2026-07-14 | Unique fields treat null as absent: multiple null values never conflict. | This matches SQL unique-index semantics, keeping the in-memory contract and the future SQLAlchemy adapter in agreement. |
| 2026-07-14 | Expression comparisons are limited to `==`, `!=`, `<`, `<=`, `>`, `>=`; membership `in` and identity `is` are rejected at compile time with `TIDE308`. | Operators the runtime and the future SQL translator cannot honor must fail at authoring time, not at evaluation. |
| 2026-07-14 | Compilation separates error and warning severities; errors fail compilation while warnings remain attached to the normalized model and CLI output. | Advisory diagnostics must be visible without making every non-fatal condition block application startup. |
| 2026-07-14 | Actions fail closed unless they declare a permission or explicitly set `unrestricted: true`; omission is `TIDE226`. | Accidentally omitting metadata must never grant mutation authority to every principal with record read access. |
| 2026-07-14 | Reference values are normalized against the target primary-key type and checked for existence before commit. | The in-memory contract must reject broken relationships before the SQL adapter adds database foreign-key enforcement. |
| 2026-07-15 | Database ownership is explicit: `managed` permits reviewed TIDE migrations, while `legacy` maps externally owned tables and columns under a hard no-DDL rule. | Existing third-party schemas must be usable without risking an implicit create, alter, drop, or migration operation. |
| 2026-07-15 | The first durable repository uses synchronous SQLAlchemy Core tables generated from the normalized model, not ORM domain classes. Construction never emits DDL; managed creation is explicit and legacy creation is refused. | Services already operate on typed dictionaries and `RecordSession`; Core preserves that boundary and makes schema authority visible. |
| 2026-07-15 | Repository queries receive validated `QuerySpec` values and row-policy criteria; SQL adapters must translate supported root predicates, ordering, and limits or fail closed during preflight. | Persistent security cannot load a root table and silently rely on Python post-filtering, while memory and SQL still need equivalent observable results. |
| 2026-07-15 | Create row policies evaluate finalized candidate values, and update policies are repeated in the persistence mutation predicate. | Authorization checked only when an edit session opens can become stale before commit, particularly on externally owned tables without version columns. |
| 2026-07-15 | Microsoft SQL Server through SQLAlchemy Core and `mssql+pyodbc` is the first multi-user database target; SQLite remains the fast local/test adapter and PostgreSQL follows after the SQL Server contract stabilizes. | SQL Server is the primary database used by the project owner, and early dialect tests prevent SQLite-specific schema, expression, identity, and concurrency assumptions from becoming architectural constraints. |
| 2026-07-15 | List continuation uses deterministic keyset predicates and opaque, expiring, server-side cursor state bound to the secured query and principal; the initial store is process-local behind a replaceable protocol. | Keyset paging avoids offset drift and cost, while random stateful tokens avoid exposing protected boundary values or inventing a cryptographic token format before deployment key management is defined. |
| 2026-07-15 | Collection hydration is driven by a bounded repository load plan that requires source-field and target-entity read access and applies target read-row policies in child and supported aggregate/reference SQL. | Loading every child before projection leaks protected rows into the runtime, while silent truncation makes business data incorrect; explicit policy predicates and fail-closed depth/item limits preserve both security and completeness. |
| 2026-07-15 | The first OpenAPI milestone is an adapter-independent 3.1 preview built from generated Pydantic record/page models and restricted to explicitly exposed list/get routes. | Contract generation can be tested with the secured core before FastAPI hosting exists, while mutation transport and authentication remain separate reviewed milestones. |
| 2026-07-15 | Idempotent actions reserve a durable key immediately before handler execution, and audited actions persist a started-to-terminal lifecycle; failed or interrupted keys require reconciliation rather than automatic re-execution. | The framework cannot prove whether an interrupted handler produced external effects, so availability must not be preferred over duplicate mutation risk. |
| 2026-07-15 | Multi-process continuation uses an explicitly managed SQLAlchemy cursor store with exact typed server state, transactionally bounded TTL/capacity, and only SHA-256 hashes of random bearer tokens at rest; the in-memory store remains the local default. | Restarts and load-balanced processes must share pagination state without exposing usable tokens or weakening the legacy database no-DDL boundary. |
| 2026-07-15 | Textual is an optional `tui` adapter dependency; `tide run --demo` may execute only the selected application's fixed `demo_data.py` provider and loads it into an in-memory repository. | Headless deployments should not require UI packages, while a deliberate demo flag provides a visible, database-safe golden slice without embedding application fixtures in the framework. |
| 2026-07-15 | `tide run` may execute an application's fixed `runtime.py` registration hook, whose only framework contract is `configure_runtime(records, actions)`. | Ordinary Python business behavior remains application-owned and explicit, while Textual, future REST, and MCP adapters invoke the same registered generators and action handlers through secured services. |
| 2026-07-15 | Reference fields use compact selectors by default and may opt into a resolved multi-column lookup view; declarative selection assignments copy secured related values into writable drafts through application services. | Small lists should remain quick, large business references need searchable tables, and copied prices must be editable historical snapshots shared consistently by TUI and future GUI adapters. |
| 2026-07-16 | `tide run --database-env` reads a deployment URL only from a named environment variable; managed schema creation requires the separate `--create-schema` flag. Managed runtimes use database-backed cursor/action state, while legacy runtimes retain process-local framework state without adding objects to the external schema. | Application metadata and CLI output must not carry secrets, startup must never perform implicit DDL, and legacy compatibility must not weaken the hard no-structure-change guarantee. |
| 2026-07-16 | Development fake-data profiles are application-owned Python providers invoked only by an explicit `tide db seed` command against an empty managed database; generated records pass through secured services and real actions. | Faker can supply realistic repeatable fixtures without putting business-specific generation in the framework or allowing normal startup to mutate persistent data. |
| 2026-07-16 | Reference views may declare `allow_create` plus a compiler-validated target form; nested **Save & Select** commits the master record independently and applies it to the preserved parent draft only when runtime create authorization succeeds. | Business data entry often discovers missing master data mid-transaction, while permissions, reusable adapter semantics, and explicit transaction boundaries must remain intact. |
| 2026-07-16 | Decimal precision/scale and completed-value regular-expression masks are authoritative service contracts; renderers may add typed numeric masks and early feedback without owning validation. | TUI, future Qt/web clients, and APIs must accept and reject the same values, while each surface remains free to provide an idiomatic editing control. |
| 2026-07-16 | The first executable report kind is a compiler-validated single-record query that builds an immutable secured `ReportDocument`; Textual previews it, standard Python writes print HTML, and the optional ReportLab adapter writes A4 PDF. | Primary-key retrieval stays indexed and row-policy-aware on SQL Server and legacy databases, protected fields fail closed, and future GUI/web surfaces can reuse report semantics without depending on Textual or a browser-to-PDF process. |
| 2026-07-16 | FastAPI is a transport boundary over TIDE services, not a replacement service layer. Routes are opt-in; mutations use typed writable projections, `PATCH`, strong ETag preconditions, and action idempotency. The fixed-token development identity may bind only to loopback, and roles are assigned at server startup rather than accepted from clients. | Future remote TUI, Qt, web, and MCP clients should share one secured server without database credentials; partial updates cannot overwrite omitted/protected fields, concurrent changes fail visibly, and production network exposure waits for HTTPS plus a reviewed OAuth/OIDC adapter. |
| 2026-07-16 | Remote clients authenticate against a versioned session-capability resource, validate it against their compiled application, and centralize wire types, protected values, cursors, ETags, and errors in a reusable client adapter. Plain HTTP is loopback-only and redirects are not followed. | TUI and future Qt clients need rendering hints without treating them as authority, must fail visibly on dependency/model version drift, and must never leak bearer credentials through an unencrypted origin or redirect. |
| 2026-07-16 | Remote Textual mode keeps presentation metadata and transient `RecordSession` drafts client-side, but routes structured queries, reference assignments, commits, and actions through service-shaped HTTP facades. Unsupported remote reports fail closed. | Renderers need responsive local form state without receiving database access or duplicating authoritative validation/security; unavailable transports must disable features rather than silently bypass the server. |
| 2026-07-16 | Remote reports transport a versioned renderer-neutral `ReportDocument`; authorization, secured data loading, formatting, and protected-field checks occur on the server, while Textual performs preview and optional local HTML/PDF rendering. | Raw records must not move to a client merely to generate a report, while renderer-neutral output preserves future Qt/web reuse and avoids coupling the API to one binary export format. |
| 2026-07-16 | Production bearer authentication uses provider-neutral OIDC discovery and JWKS validation with exact issuer/audience, asymmetric algorithm and token-type allow-lists, required expiry/subject/key ID, and explicit external-to-TIDE role mappings. Development identity stays loopback-only; non-loopback serving requires direct Uvicorn TLS. | TUI, future Qt/web, and automation clients can share a standard identity boundary without database credentials or client-selected roles, while fail-closed validation and encrypted transport precede network exposure. Interactive login/refresh and trusted reverse-proxy configuration remain separate reviewed contracts. |
| 2026-07-16 | Runtime MCP begins with metadata-opted schema/record resources and structured search tools on the stable v1 official SDK, using authenticated stateless Streamable HTTP beside REST. The canonical resource URL drives RFC 9728 discovery and Host/Origin protection; every call reconstructs a server-controlled `Channel.MCP` context and reuses `RecordsService`. | Read-only AI access can be tested before writes without creating a parallel database/security path. Exact protected values and principal-bound cursors stay consistent across REST and MCP, while mutations/actions wait for concurrency, idempotency, and audit transport contracts. |
| 2026-07-16 | Developer MCP is a separate local stdio server. It exposes compiled project resources and accepts discriminated logical operations for deterministic, approval-required application proposals, but has no source path, arbitrary Python, apply, shell, test-execution, database, or write tool. | AI-assisted generation should begin with inspectable TIDE vocabulary and semantic diagnostics, while source materialization waits for isolated compilation/testing, exact diffs, stale-base protection, explicit approval, comment-preserving edits, and audit. Runtime data authority must never imply source authority. |
| 2026-07-16 | The initial materialization stage renders new-application proposals only into a framework-created temporary tree, compiles and statically checks them, and returns exact artifacts/digests/diff bound to proposal, empty-base and candidate fingerprints. At this stage fixed transition templates were AST-parsed but not executed, used the server clock for stamps, and the candidate was deleted before return; no apply tool exists. | AI clients need compiler evidence and a fully reviewable change unit before approval. Confinement, deterministic output and cleanup preserve the no-workspace-write boundary; the following decision separately expands the preview to fixed-template integration checks. |
| 2026-07-16 | Candidate preview now generates conventional views and may execute only reviewed transition/sequence templates after compiler/static success, using fresh in-memory services with bounded fixture depth/counts. It checks authorization denial, CRUD, idempotent actions, secured reports, HTML and optional PDF, runs no external command, opens no configured database, discloses execution flags, and deletes the candidate. | A generated application needs runtime evidence before approval, while the proposal schema's lack of code/module/path inputs makes fixed-template execution materially different from custom-code execution. Temporary/in-memory isolation is not an OS sandbox; custom code remains prohibited and actual destination/stale-base approval is still required before apply. |
| 2026-07-17 | New generated applications may be published only through a separate local approval service and interactive CLI. Approval binds proposal, canonical `applications/<id>` destination, absent-base and candidate fingerprints; apply regenerates, revalidates, locks, stages, byte-checks, recompiles and renames the tree, refuses existing/case-colliding/unsafe targets, and records `.tide-apply.json`. Developer MCP remains no-write. | Preview output alone is not informed consent. A conspicuous human command, stale-base recheck, no-overwrite rule, cleanup, exact receipt and source-control diff provide a narrow auditable path without granting an AI arbitrary file or existing-application edit authority. |
| 2026-07-17 | Existing-application design begins with a no-write headless `DesignerService`: typed commands address semantic documents and bounded model paths, atomic batches form undo units, and round-trip YAML preserves comments, quotes and order while temporary candidates are compiled and diffed. | Textual, Qt, Web and AI clients need one safe editing contract. Separating in-memory design from stale-base-approved atomic save keeps invalid intermediate states recoverable and prevents a preview-capable client from silently changing source files. |
| 2026-07-17 | Existing YAML sources may be changed only through a separate local `DesignerSaveService`: preparation binds canonical project, live base, candidate, artifact and diff fingerprints; interactive save locks, restages, byte-checks, recompiles, rechecks each target, replaces files atomically one at a time with reverse rollback, and writes a `.tide/designer/` receipt. Python, source inventory changes and MCP-side save remain prohibited. | A typed edit proposal is not write authority. Exact human approval plus optimistic concurrency, a narrow YAML-only mutation surface, preserved originals and auditable receipts gives all future Designer clients one persistence contract without giving AI or presentation clients arbitrary filesystem access. |
| 2026-07-17 | Designer saves create a strict transaction lock held by an OS byte lock and an fsynced phase journal before source mutation. Explicit recovery reacquires the lock, validates canonical records and actual file hashes, then permits only evidence-bound reverse rollback or cleanup of a fully receipted candidate; it is resumable and fails closed on ambiguity. | A process can disappear between individually atomic replacements. Journal hints alone cannot prove filesystem state, timeouts cannot prove a writer is dead, and blindly rolling back can undo a completed save. OS ownership plus byte-derived recovery preserves safety across interruptions without granting a recovery command arbitrary repair authority. |
| 2026-07-17 | The first Textual Studio editor applies only type-preserving scalar-leaf changes to its process-local `DesignerService` session and exposes validation, diagnostics, undo/redo, exact diff, schema-derived choice/Boolean controls, searchable previews and optionally syntax-colored YAML. Containers, schema version and semantic identities remain locked; closing discards changes until the approved save service is explicitly connected. | A useful visual milestone can exercise the shared command and source-schema models without inventing presentation-side mutation semantics or weakening source authority. Structural edits and renames need atomic cross-document commands, while experimentation must remain safe before persistence UX is reviewed. |
| 2026-07-17 | Studio expert YAML editing uses a bounded whole-document Designer command that strictly parses one existing source, preserves its semantic identity, recompiles in memory and participates in ordinary undo/redo; the editor has no direct save authority. | Experienced developers need a fast searchable source workflow, but raw text must not bypass compiler validation, cross-document rename requirements, source-size limits, exact diff review, or the separately approved persistence boundary. |
| 2026-07-18 | Textual Studio may persist only a valid dirty candidate through an exact-diff review and complete evidence-bound approval phrase passed to the existing `DesignerSaveService`; success reopens a clean session, while stale bases and save locks expose no-write blockers and recovery-preview guidance. | Closing the visual editing loop should reuse the already reviewed transactional YAML-only boundary rather than give widgets filesystem authority. Fresh sessions prevent undo from silently crossing a saved baseline, and recovery remains a separate explicit operation. |
| 2026-07-18 | The first structural Studio contract exposes compiler-resolved table/lookup columns and form/inline left-right field tracks with provenance; local fields may move only within their current track through bounded sequence moves or atomic slot swaps. | A small structural vocabulary provides immediate field-ordering value and can be shared by Textual, Qt and Web without premature free-form layout semantics. Keeping inherited/generated tracks read-only and routing every move through compile, diff, undo and approved save preserves layered metadata ownership. |
| 2026-07-18 | Structural cross-column changes are same-position swaps limited to local slots in the same YAML group; local field add/remove operates only on existing entity fields and updates inline columns/layout together. | Current layout metadata has no portable empty-cell concept, so unrestricted transfers could silently change other field columns or group ownership. Conservative swaps and atomic membership edits are predictable, undoable, compiler-validated, and do not imply entity/schema/database deletion authority. |
| 2026-07-18 | Studio field additions to form/inline views require an explicit local destination group; group management permits bounded labels, create/rename, one-adjacent-group reorder, and empty-only removal, with collection sections acting as fixed barriers. | Explicit destinations remove selection-dependent ambiguity. Collection-aware barriers and empty-only deletion preserve layout ownership and prevent a group operation from implicitly moving collections or deleting fields before those structures have their own reviewed designer contract. |
| 2026-07-18 | Portable form structure uses optional repeated `tab` labels on layout sections, ordered form actions, and ordered collection actions; Studio edits complete section/collection/action tracks and Textual renders the same compiled contract. | Tabs and button placement must be application semantics reusable by future Qt/Web renderers, not a Studio-only illusion. Compatible inline-view checks, bounded action vocabularies, undo/diff/compiler validation, and the existing approved save boundary keep structural edits safe. |
| 2026-07-18 | Studio role/terminal previews are static compiled-model projections: a synthetic TUI principal uses the shared `SecurityEngine`, while fixed terminal presets evaluate declared/estimated layout constraints without records, database access, or application-code execution. | A preview should reveal permission and sizing mistakes without becoming a second authorization implementation or leaking real data. Record-dependent field/action rules must remain explicitly conditional until a real secured record is evaluated by runtime services. |
| 2026-07-18 | Record deletion is a secured application-service operation and exists over REST only when both `expose.rest.operations` and `permissions.delete` explicitly allow it. Versioned records require the observed version; root row criteria remain in the atomic delete; `restrict`, `cascade`, and `set_null` execute transactionally in both repository adapters, including application-side SQL behavior for legacy no-DDL mappings. | Delete is higher-risk than ordinary editing and must fail closed at capability, authorization, row, concurrency, and reference boundaries. Application-side relationship behavior preserves one metadata contract without requiring TIDE to alter third-party schemas, while existing database constraints remain a final integrity boundary. |
| 2026-07-18 | Textual record deletion is rendered only when the resolved browse action list contains `delete` and the shared security view grants it. `confirm_delete` presents the record display value with cancellation focused by default; confirmation passes the selected record's observed version to `RecordsService`, refreshes on success, and explains stable relationship restrictions. | Destructive UI must reflect developer-authored presentation and server-enforced authority without becoming an alternative deletion path. A safe second gesture, useful conflict text, and identical local/remote service facades make the operation testable while preserving renderer independence. |
| 2026-07-18 | Stale-edit inspection and resolution use shared three-way Original/Current/Draft contracts. Textual requires an explicit Current/Mine choice for every genuine overlap, then copies the completed plan into a fresh `RecordSession`; the dialog never writes storage, and current permissions plus workflow immutability are reevaluated before any draft value is retained. | Conflict semantics must be reusable by future Qt/Web clients and remain subordinate to service enforcement. Explicit choices avoid silent data loss, while a concurrent workflow transition can still make a concurrency-safe value illegal, so every resolved draft must pass current business rules and normal validation. |
| 2026-07-18 | Record action history is exposed only through a bounded `AuditHistoryService` guarded by explicit `permissions.audit`; REST requires record `get` exposure, remote capabilities disclose only availability, and public projections omit payloads, protected values, raw keys, and stored key hashes. | Audit visibility is distinct from record read/write authority. One renderer-neutral service keeps local TUI, remote TUI, FastAPI, and future clients consistent while fail-closed metadata and non-disclosure prevent the diagnostic surface from becoming a data-leak path. |
| 2026-07-18 | Successful root CRUD mutations are audited inside `RecordsService` into a separate TIDE-owned `tide_record_audit` table; fields default to name-only capture and require `audit: values` for bounded before/after values, while read-policy fields are redacted before storage and rechecked at history read time. | Capturing at the shared service boundary prevents renderer bypass, a separate table avoids altering the existing action lifecycle or legacy application tables, and two-layer redaction keeps operational history useful without turning it into an alternate protected-data channel. |
| 2026-07-19 | Runtime MCP mutations are independently metadata-opted and use the same generated writable inputs and services as REST; domain actions remain separately exposed. Versioned calls require an observed version, idempotent actions require a key, mutation results carry exact protected wire values and correlation, and audit history has its own permission. | An AI client may automate useful business work without receiving database credentials or a parallel validation path, while explicit capability, authorization, concurrency, idempotency, protected-value, and audit boundaries keep omission and retries fail-closed. |
| 2026-07-19 | HTTP liveness is process-only, while readiness probes repository connectivity, schema/policy compatibility, and configured durable runtime-state schemas. Both routes are unauthenticated for infrastructure use but return only bounded application identity and ready/not-ready state; readiness failures are HTTP 503 and never expose dependency exceptions or perform repair. | Supervisors need probes before application login is available, but operational endpoints must not become a credential, topology, schema, or migration-information leak. Separating liveness from dependencies avoids restart loops while fail-closed readiness prevents traffic reaching an incompatible process. |
| 2026-07-19 | Hosted HTTP requests receive a bounded validated or server-generated `X-Correlation-ID`, propagated through REST and MCP `RequestContext` into service audit. TIDE emits allowlisted JSON operational events using stable operations and disables duplicate Uvicorn access logs; bodies, headers, query values, raw paths, cursors, prompts, SQL values, and exception messages are excluded. | Operators need one safe identifier across transport and durable business history, but conventional access/error logging can disclose identifiers, secrets, and payloads. A narrow framework-owned schema provides useful telemetry without making logs an alternate data channel or audit system. |
| 2026-07-19 | REST and hosted MCP share a typed HTTP-host limit contract: declared and chunked bodies are size- and receive-time-bounded before parsing, concurrency and connection lifecycle have conservative defaults, rejected bodies return safe correlated 408/413 errors, and OpenAPI discloses the active body controls. Proxy headers and the server-identification header remain disabled. No hard application-execution timeout is claimed until synchronous database cancellation is dialect-certified. | Transport limits must cover every adapter before authentication and payload parsing without creating a second service path. A pre-database body-read deadline is safely cancellable, but a cancelled application waiter cannot guarantee that driver work stopped; explicit bounded controls and honest timeout semantics fail more safely. |
| 2026-07-19 | TIDE's first executable recovery artifact is a non-overwriting SQLite online snapshot bound to an adjacent application/version/mode/size/SHA-256 manifest and verified through SQLite integrity plus compiled application/framework schema checks. Server databases retain database-native backup authority; SQL Server acceptance requires a real isolated restore followed by `tide db check`, not only `RESTORE VERIFYONLY`. | A row export is not a transactionally complete server backup, application credentials should not receive infrastructure recovery authority, and backup creation alone does not demonstrate restorability. The initial command is useful and safe for local SQLite while preserving a credible DBA-owned recovery boundary for the primary multi-user target. |
| 2026-07-19 | `tide db diff` owns deterministic migration semantics before Alembic: it reflects managed application/framework tables, classifies differences as additive, data-required, destructive, or manual, fingerprints ordered intent, never infers renames, and performs no writes. In legacy mode it becomes a no-DDL compatibility report. Revision generation and apply remain unavailable until explicit rename/destructive approval and stale-schema contracts exist. | Generic schema autogeneration cannot know object identity or business-safe data conversion. Separating inspection and semantic approval from revision rendering prevents accidental drop-and-add renames, keeps externally owned schemas protected, and creates an auditable input for future dialect-specific Alembic execution. |
| 2026-07-19 | Managed tables and persisted columns may carry globally unique dotted `migration_id` values and explicit `renamed_from` physical names. The compiler rejects ambiguity and legacy rename intent; `db diff` recognizes declarations, normalizes participating key/foreign-key/index comparisons, and still classifies rename execution as manual without generating DDL. | Stable identity lets a reviewed proposal preserve data across physical renames while keeping similarity heuristics, ambiguous old/new states, and externally owned schema changes fail-closed. |
| 2026-07-19 | `tide db revision` is a render-only approval boundary: it re-inspects the database, requires exact base/proposal fingerprints, bounded backup evidence and every non-additive change key, rejects partial/unsupported output, confines non-overwriting artifacts to the application, and emits an Alembic-compatible script plus SHA-256 manifest. It has no apply authority. | A reviewable script is useful before execution exists, but generic autogeneration, stale proposals, broad confirmation flags, missing backfills, ambiguous constraint names, or an output file alone must never be treated as migration approval. |
| 2026-07-19 | Offline SQL rendering uses the dialect fixed in a verified revision manifest and Alembic's connectionless operation context. TIDE never imports the revision: it checks a strict module template and allow-listed operation AST, then emits non-overwriting direction-specific SQL plus a manifest bound to both source hashes. SQLite nullability changes remain blocked pending batch-rebuild semantics. | DBA review needs exact vendor SQL without granting application credentials or Python execution. Hash verification alone is not an authenticity boundary, while generic SQLite ALTER output would be misleading; source control/signing and dialect-specific structural limits remain explicit. |
| 2026-07-20 | Python 3.11 is the sole required CI interpreter during early development, tested on Ubuntu and Windows. Feature branches run only the pull-request workflow; pushes run CI only on `main`. `requires-python >=3.11` remains permissive, but newer interpreters are best-effort until compatibility testing is deliberately expanded. | Two operating systems protect the important platform boundary while removing redundant interpreter combinations and duplicate push/PR matrices that add cost and flaky noise before TIDE has a stable public compatibility contract. |
| 2026-07-20 | The first `kind: summary` report translates direct comparison criteria into secured `RecordsService` queries, groups projected rows in the report service, and supports `count` plus numeric `sum`. Its maximum 500-row source bound fails closed instead of returning partial totals. CSV renders only the authorized formatted detail table, uses UTF-8 with BOM on disk, and neutralizes formula-looking text. | This delivers a useful operational report without arbitrary SQL, unbounded in-memory scans, misleading aggregates, or spreadsheet formula injection, while keeping the renderer-neutral contract reusable by TUI, REST, Qt, and web clients. |
| 2026-07-21 | The first Qt vertical slice is an optional PySide6, read-only remote browse. A Qt-neutral controller derives accessible views, columns, formats, reference labels, and cursor pages from the compiled model and authenticated session; the widget process receives an API URL and bearer token but no database URL or driver. | This tests the native-GUI direction against the existing security/service boundary without prematurely duplicating form, workflow, report, login, or packaging behavior. Keeping PySide optional and the controller independently testable avoids imposing a large desktop dependency on servers and ordinary CI. |
| 2026-07-21 | Qt read-only detail opens a selected browse identity through the authenticated `get` contract and renders compiled form groups plus inline collection columns. View, double-click, and Enter invoke one path; actions and editors are absent. | A useful Invoice inspection flow should include line items without introducing a second data path or pretending mutation semantics are ready. Reusing form metadata and protected/reference formatting tests cross-renderer structure while keeping the next editing boundary explicit. |
| 2026-07-25 | Qt and Textual browse lists treat the resolved `page_size` as a bounded fetch batch and append opaque cursor results near the visible scroll boundary; Previous/Next controls are omitted, loaded rows remain available when scrolling back, and query changes start a new cursor sequence. Blocking renderer requests run outside the UI thread. | Business lists should feel continuous without loading an unbounded table or giving renderers database knowledge. One cursor-backed server-mode interaction scales to large datasets, keeps local and remote TUI behavior aligned with Qt, and preserves server-side security, filtering, and sorting. |
| 2026-07-25 | Browse search fields, queryable named filters, and sortable columns are derived by a renderer-neutral metadata helper. Qt debounces text search, maps named-filter selection and sortable header clicks to typed server queries, and ignores results from stale query generations. | Textual and Qt should interpret the same YAML consistently, while filtering and ordering must apply to the complete secured result rather than only locally loaded rows. A small immutable query state makes cursor resets and asynchronous response ownership explicit. |
| 2026-07-25 | Qt column order and widths may be personalized in local settings scoped by application, view, and authenticated principal. Layouts are versioned and stored by stable field name; Best Fit is explicit, and Reset removes the override. Personalization never edits YAML, while publishing a shared default remains a separate privileged Designer operation. | End users need durable desktop layouts without accidentally redefining application metadata for everyone. Field-name reconciliation tolerates ordinary metadata additions/removals, local storage avoids a premature server preference contract, and the existing approved Designer save boundary remains authoritative for portable defaults. |
| 2026-07-25 | The first editable Qt contract is limited to flat scalar forms and is enabled only by compiled form metadata plus authenticated create/update capabilities. Product and Customer use metadata rows, typed/masked controls, column-first traversal, background API calls, and observed ETags; authoritative permission, row, normalization, validation, and uniqueness checks stay on the server. Invoice collections, references/lookups, actions, and conflict review remain read-only/deferred. | A useful write milestone should prove the shared FastAPI mutation boundary without prematurely duplicating the mature Textual master-detail workflow. A narrow flat-form boundary is testable, preserves UI responsiveness and Decimal precision, and fails closed for unsupported form structures while establishing reusable Qt editor primitives. |
| 2026-07-25 | Qt reference editing requires a compiler-resolved lookup view and target list capability. Search uses bounded structured API queries across declared readable fields; selection goes through the shared server reference-selection operation. When metadata allows creation and target create capability is present, the compiled target form opens with **Save & Select**. Invoice header create/update may omit collection sections only with an explicit notice that an existing collection stays unchanged or a new one starts empty. | Reusing the existing lookup and `on_select` contract keeps Qt, TUI, future Web UI, and API behavior aligned while preserving server-owned authorization and defaults. Making Invoice header editing useful now proves nested creation without pretending that transactional InvoiceLine master-detail editing or conflict review is complete. |
| 2026-07-25 | Qt editable collections require a compiler-resolved inline view, writable parent collection, and target nested create/update capability. Add/Apply/Remove modify only the local draft; Product selection uses the shared reference-selection operation; computed totals are previews; final Save filters non-input nested fields and performs one ETag-protected parent mutation. Without nested capability, the collection remains visible and read-only. | This completes the Invoice master-detail proof without a GUI-specific persistence path. Explicit local line actions make draft state understandable, nested input filtering matches the service payload boundary, and server-side validation, recomputation, authorization, concurrency, and transaction ownership remain authoritative. |
| 2026-07-25 | Qt stale updates reuse the shared Original/Current/Draft comparer. The GUI requires an explicit Current/Mine choice for every genuine overlap, summarizes a nested collection as one conflict unit, reevaluates current workflow locks, and reopens the resolution on the latest ETag for normal review and save; the conflict dialog never writes. | Textual and Qt should have identical loss-prevention semantics without moving concurrency policy into widgets. A fresh form makes the resolved values visible, keeps current permissions and validation authoritative, and avoids unsafe automatic row-level merging of transactional child collections. |
| 2026-07-25 | Qt form-domain actions come from the compiled form action order intersected with authenticated session capabilities. Local `visible_when`/`enabled_when` evaluation is advisory; execution saves a changed draft first, passes the returned ETag plus a unique idempotency key to the existing FastAPI action route, and reopens an already-saved draft if the action then fails. A read/action-only principal may open a form with every editor read-only and Save hidden. | Native actions must not become a GUI-only command or weaken the shared security, concurrency, idempotency, validation, and audit boundary. Chaining the server-returned version prevents a save-then-action self-conflict, recovery avoids leaving the user on an obsolete form after a partial two-request workflow, and action permission must not accidentally imply update permission. |
| 2026-07-25 | Qt record-report availability is the intersection of the active entity, record kind, explicit REST exposure, and authenticated session report capabilities. FastAPI builds the immutable `ReportDocument`; Qt renders it to a unique GUI-session file under the operating system temporary directory outside the GUI thread and opens the system PDF viewer. | Native preview must not duplicate report queries, move authorization to the desktop, expose database credentials, or accumulate ordinary previews as durable exports. Reusing the renderer-neutral document keeps Textual, Qt, and future web output semantically aligned while allowing each presentation layer to provide an appropriate preview. |
| 2026-07-26 | Form layout rows, groups, collections, tabs, action order, and hidden-field decisions are resolved once by a renderer-neutral presentation helper. Textual and Qt consume that result; renderer-specific settings may change measurements but not semantic placement. Portable `settings.compact_groups: true` retains authored groups for Studio while resolving their scalar fields into one ordered two-column header before collections. Qt browse exposes one **Open** path, and the resulting form enables or hides fields, Save, reports, and domain actions from metadata, workflow state, and authenticated capabilities. | Raw-YAML interpretation had drifted: Textual flattened fields while Qt rendered literal four-column rows and extra groups. A shared semantic contract prevents the future Web renderer from becoming a third interpretation, while one record window avoids separate View/Edit/Preview modes that can disagree about the same record state. |
| 2026-07-26 | Qt detail-form Previous/Next navigation follows the current secured browse query and its server-side sort/filter order; Page Up and Page Down invoke the same guarded actions at dialog scope. It resolves only loaded list identities, fetches an opaque next cursor batch when adjacency crosses that boundary, loads the target through the authenticated record API, and replaces title, values, ETag, editability, actions, collections, and navigation state within the same positioned dialog. It keeps the source form intact on failure. A locally changed draft must be saved or cancelled before navigation. | Record-to-record navigation should improve review speed without becoming page navigation, a database path, a flickering close/reopen cycle, or a source of silent data loss. Reusing the active cursor-backed query preserves renderer/service boundaries and makes the visible list the authoritative navigation context. |
| 2026-07-26 | Application navigation is an ordered, compiler-validated presentation contract of grouped browse views. Renderers intersect it with authenticated list capabilities; Textual uses its order and labels, Qt renders a grouped sidebar and lazily retains each visited workspace, and `--view` remains an accessible deep link without rewriting YAML. | TUI, Qt, and future Web need one application map rather than renderer-owned menus. Capability filtering prevents navigation from implying authorization, while retained per-view state makes desktop switching useful without mixing portable defaults with personal query or column preferences. |
| 2026-07-26 | Qt exposes an active entity's parameterless summary report only when it is REST-exposed and present in authenticated session capabilities. It builds the server-owned `ReportDocument` outside the GUI thread and opens a native read-only preview with controlled CSV, HTML, and PDF exports; current browse filters and loaded rows do not rewrite the report query. | This closes the existing Textual/REST/Qt parity gap without introducing a Qt reporting engine, client-side aggregation, or premature parameter-designer scope. Authorization, row policies, bounded source queries, formatting, and protected values remain authoritative on the server. |
| 2026-07-26 | Remote renderers discover application navigation and browse behavior through an authenticated, versioned, principal-bound presentation manifest. The server projects only REST-exposed and list-capable views, readable columns, safe structured named filters, search/sort controls, formats, reference targets, paths, batch size, and current operations; inaccessible groups and field-dependent controls are removed. Raw YAML, permission expressions, Python handlers, and database configuration remain server-side, and every data request is reauthorized. | A browser needs enough normalized metadata to share TUI/Qt semantics without becoming a source-code interpreter or authorization authority. A narrow fail-closed projection establishes the Web shell contract while preserving the existing FastAPI service and security boundary. |
| 2026-07-26 | The first Web renderer is a generic React/TypeScript application built with Vite, Tailwind CSS, code-owned shadcn/ui controls, and TanStack Query/Table/Virtual. It consumes only the safe presentation manifest and generated REST routes; development tokens stay in runtime memory, while non-sensitive theme and personal column order/widths may use browser storage scoped by application, principal, and view. Portable YAML expresses semantic presentation intent rather than Tailwind classes. A built renderer may be hosted at the FastAPI origin after API routes, with immutable caching limited to fingerprinted assets. | This provides a modern reusable browser surface without duplicating authorization or binding metadata to one component library. Code-owned controls remain adaptable, same-origin hosting simplifies deployment and avoids CORS, and separating shared YAML defaults from personal layouts preserves the existing cross-renderer designer contract. |
| 2026-07-26 | The Web detail contract projects only readable renderer-neutral form rows, tabs, and authorized collection columns through the principal-bound presentation manifest. Record `GET` may add server-evaluated `writable_fields` hints; raw workflow and permission expressions remain server-side, and mutation routes must reauthorize. Web opens through one selected-row path, retains the browse workspace while hidden, and replaces only record data during Previous/Next navigation in the current cursor-backed query. | Cross-renderer form parity requires one compiled layout without making the browser a YAML interpreter or authorization authority. A stable in-place shell avoids flicker and preserves list state, while advisory field state clearly distinguishes workflow locks and still fails closed when no field is writable. |
| 2026-07-27 | The first Web mutation slice is limited to compiler-resolved flat scalar forms and generated create/update routes. The safe presentation manifest adds required/help state, choices, bounded masks, numeric constraints, validation names, resolved defaults, and principal-level writable hints without source expressions. The browser keeps Decimal input as text, applies advisory early validation, sends changed fields only, returns an observed ETag in `If-Match`, and maps safe server validation issues back to fields. Unsupported references, collections, actions, and conflict resolution remain read-only. | A bounded Product/Customer slice proves that modern Web controls can share TIDE metadata and FastAPI services without moving authorization or business rules into React. Exact values and optimistic concurrency survive the browser boundary, while structured issues improve usability without exposing source metadata or changing the stable non-validation error envelope. |
| 2026-07-28 | Cross-renderer parity is tracked in a versioned executable YAML matrix. A required parity tier must be covered by Textual, Qt, and Web and every covered cell must resolve to automated evidence. The compiled Invoicing navigation, browse order/alignment, and form-section golden contract is compared with the authenticated Web presentation projection; roadmap-tier cells explicitly retain renderer gaps. | “Shared metadata” is meaningful only if renderer drift becomes a failing check rather than a documentation claim. Separating the green semantic baseline from lookup, master-detail, conflict, action, report, and renderer-specific layout gaps keeps the matrix honest without forcing different technologies into identical pixel or widget behavior. |
| 2026-07-29 | Web scalar reference editing consumes a capability-filtered compiler-approved lookup contract from the authenticated presentation manifest. It searches declared readable columns through ordinary structured REST queries, applies selection and `on_select` values only through the server reference-selection operation, and offers nested **Save & Select** only when metadata, target create capability, and a safe target form agree. Invoice header editing may keep collection rows visible but unchanged. | This closes the Customer lookup parity gap without making React a YAML interpreter or authorization authority. Independent master creation preserves the parent draft and matches Textual/Qt semantics, while explicitly deferring collection mutation avoids pretending Product selection is safe before the one-draft/one-transaction Web master-detail contract exists. |
| 2026-07-29 | Web editable collections require a compiler-resolved inline view, writable parent collection, readable child identity, target nested create/update capability, and writable child fields. The safe manifest supplies editor rows and YAML Add/Apply/Remove order without expressions. Product lookup and nested **Save & Select** reuse the server reference-selection operation; changed children remain local until one complete ETag-protected parent replacement, after which the server validates, handles orphans, recalculates stored values, and commits transactionally. | This closes the Invoice master-detail parity gap without adding child REST mutations or making React responsible for workflow, computed expressions, or transaction boundaries. The same InvoiceLine YAML now drives Textual, Qt, and Web, while a missing capability keeps the collection visible only when its readable columns are safe and never advertises an editor. |
| 2026-07-30 | Web stale updates reuse the shared Original/Current/Draft semantics: the browser rereads the latest secured record, requires an explicit Current/Mine choice for every genuine overlap, treats a nested collection as one conflict unit, and reevaluates current `writable_fields`. Applying a complete resolution only rebases permitted draft values onto the fresh record and ETag for review; a separate Save repeats the ordinary FastAPI mutation path. | Conflict assistance must prevent lost updates without becoming a client-side merge authority or bypassing current workflow rules. Keeping comparison write-free, dropping newly locked values, and requiring an ordinary second save preserves the shared service, authorization, validation, and concurrency boundary. |
| 2026-07-30 | Web form-domain actions come from the capability-filtered presentation action order, but the manifest exposes only safe names, labels, and idempotency requirements. Record responses carry server-evaluated visibility/enabled hints without source expressions. A changed draft may make a previously disabled action available, so it is saved first; Web rechecks the returned action state, then executes with the returned ETag and a unique per-attempt idempotency key before replacing the open form with the result. | Browser hints improve usability but never authorize execution: FastAPI and `ActionService` repeat permission, workflow, concurrency, validation, idempotency, and audit enforcement. Rechecking fresh state and chaining its ETag avoids both stale stored-state UX and a save/action self-conflict, while returning fresh writable/action state makes a posted Invoice lock in place without a second data path. |
| 2026-07-30 | Web report discovery projects only authorized REST report names, titles, record/summary kinds, owning entities, resource paths, and export formats. Preview consumes the immutable server-built `ReportDocument`; CSV, standalone HTML, and PDF downloads use separately authorized server export routes that rebuild the report. Record preview is unavailable for unsaved drafts, and parameterized summaries remain deferred. | React must not receive report queries, permission expressions, database access, or implement a parallel reporting engine. Reusing the document and existing renderers preserves Textual/Qt/Web semantics, rebuilding exports prevents client-tampered documents, and draft blocking avoids presenting a persisted report as if it included local changes. |
| 2026-08-01 | Optional production Web login uses same-origin OIDC Authorization Code with PKCE S256 as a backend-for-frontend boundary. FastAPI retains access and optional rotated refresh tokens in a bounded process-local session; React receives only a fresh opaque HTTP-only cookie and per-session CSRF proof. Login state is short-lived, single-use, browser-bound, and count-bounded. Replacement access tokens repeat exact OIDC validation and must keep the same subject. Bearer authentication remains authoritative for remote clients and hosted MCP, while development-token startup is unchanged. | Provider credentials and role claims must not enter browser JavaScript or storage, and browser convenience must still become the same service-owned `Principal`. PKCE, binding, CSRF, fail-closed refresh, and bearer/cookie separation close the initial browser threat paths without pretending that one-process memory is a scale-out session solution. |
| 2026-08-01 | OIDC browser startup and `tide auth check-oidc` share one discovery-capability contract: exact issuer and HTTPS endpoints, authorization-code response/grant, and the configured `none` or `client_secret_basic` token authentication method. Published PKCE methods must include S256; when that optional metadata is absent TIDE still sends S256 without downgrade and emits an explicit provider-enforcement acceptance warning. Optional scope/refresh metadata also produces warnings; JSON output never contains the client secret. Preflight is not claimed as proof of client registration, issued claims, mapped roles, verifier enforcement, or refresh issuance, so deployment acceptance still requires a real interactive sign-in. | A sign-in button that is known from metadata to be incompatible should fail before traffic is accepted, while optional or incompletely advertised capabilities must not be overstated or automatically treated as failure for providers that document support out of band. One reusable contract gives startup, operators, tests, and CI the same answer without handling credentials in reports or replacing the final human/provider acceptance step. |
| 2026-08-01 | Framework-owned username/password authentication is the default Web sign-in path. A separately initialized, application-bound SQLite file owns normalized users, versioned uniquely salted PBKDF2-HMAC-SHA-256 hashes, and developer-assigned compiled role names without changing the business database or YAML. Successful login becomes the same bounded opaque HTTP-only session and CSRF-protected `Principal` used by the service layer; generic failures and a small process-local failure window limit guessing. OIDC remains isolated and optional rather than a dependency or current product direction. | TIDE must be usable without Microsoft, social, cloud-tenant, or other third-party login services while preserving the one authorization boundary and legacy no-DDL guarantee. Separate credentials avoid contaminating externally owned schemas, server-assigned roles prevent privilege selection at login, and a narrow local management CLI keeps the initial operational model understandable. |
| 2026-08-02 | Query filters use SQL three-valued semantics in every repository: a stored null satisfies no comparison, including `ne`. A null *criterion* asks about presence and translates to `IS NULL` / `IS NOT NULL` for `eq` / `ne`, and matches nothing for the ordering and containment operators. | The in-memory store raised `TypeError` on ordering comparisons against a stored null, surfacing a legitimate REST or MCP query as a 500, and returned rows for `ne` that SQL excludes. Matching the database is what lets one query mean one thing regardless of the configured adapter, and it follows the existing rule that unique fields treat null as absent. |
| 2026-08-02 | The service contract suite runs against every repository implementation rather than the in-memory store alone. | Adapter divergence is only discoverable by executing the same contract twice; the repository protocol is structural, so an `isinstance` check confirms method names and nothing about behavior. |
| 2026-08-02 | Collection items carry a durable primary key in every repository, allocated by the store rather than the caller. | `on_delete: cascade` and master-detail editing both need to name a specific child row. The document-shaped adapter kept children inside the parent and allocated nothing, so a commit could not distinguish an edited row from a replacement and no cascade could be audited. |
| 2026-08-02 | A collection item is held to its own field rules: `readonly`, `write` mode, and field policies are enforced on children as they are on the owning record. Enforcement compares against the child as loaded, so echoing an unchanged value is not a write, and computed fields are exempt because the service recomputes them regardless. Child `immutable_when` is deliberately not evaluated. | Nothing evaluated a child's declarations, so a caller could set a system-written field or one a field policy reserved. Comparing against the loaded child is what keeps this compatible with every renderer, which sends collections back whole. Child `immutable_when` routinely addresses the parent (`invoice.status != 'draft'`), which is a scalar foreign key in that context rather than a record, and the owning record's own rule already covers the case. |
| 2026-08-02 | Removing an orphan runs the child's own reference contract rather than a bulk delete. | A `restrict` pointing at the removed row otherwise surfaced as the database's foreign-key error instead of `DeleteRestricted`, which is the difference between a business rule and a stack trace. |
| 2026-08-02 | Contract tests requiring master-detail shapes the bundled invoicing application lacks use a dedicated `inspection` fixture application. | Invoicing has one writable collection whose child owns no field policy, no system-written field, no collection of its own, and nothing referencing it, so several rules could only be asserted by inventing behaviour. A second small application makes them executable instead. |
| 2026-08-02 | Exact decimal storage is scoped to dialects whose driver binds `Decimal` without converting to a float. `mssql+pyodbc` and `postgresql+psycopg2` qualify; `sqlite+pysqlite` does not, and a deployment whose declared precision exceeds the 15 digits a double holds exactly fails readiness rather than rounding silently. The compiler, expression evaluator, and service boundary remain exact everywhere. | The 2026-07-14 decimal decision claimed exactness end to end, which storage does not deliver on SQLite: a `Decimal` is bound through a C double, so `0.1` at scale 18 reads back as `0.100000000000000006`. Storing values as text would make every dialect exact, but it changes ordering and comparison semantics database-side and cannot be certified against SQL Server today. Scoping the claim keeps the primary multi-user target exact, leaves the local development adapter honest about its limit, and turns a silent rounding into a refused deployment. |
| 2026-08-02 | Row policies may name the caller as `$principal`, resolved from the authenticated `RequestContext`. The accepted names are a fixed vocabulary the compiler validates, and the value is bound at evaluation rather than substituted into the expression text. | Ownership is the ordinary case for row security and was previously inexpressible, because criteria could see only the record. Binding rather than substituting keeps the compiled criteria exactly as authored and sends the value as a SQL bound parameter, so nothing caller-derived is ever parsed as an expression or concatenated into a statement. A fixed vocabulary makes a misspelled name fail at authoring time instead of matching nothing at runtime. |
| 2026-08-02 | An item may be removed from a collection only when that collection declares `orphan_delete`; otherwise the commit fails with an `orphan` validation issue. Omitting the collection from a payload still means "leave it unchanged" rather than "empty it". | Without `orphan_delete` the detached row keeps its foreign key, so the removal reverses itself on the next read and the caller is told nothing. A visible authoring error is better than a save that silently does not stick, and it keeps the document-shaped and relational adapters from disagreeing about what removal means. |
| 2026-08-02 | A cascade executes under the deleting principal's authority over the target record; it does not require a separate delete grant on each child entity. Every removed row, including cascaded ones, is reported by the repository and audited. | `cascade` is an explicit authoring statement that the child has no independent lifetime, and requiring a second permission would make it unusable — `sales.InvoiceLine` deliberately grants no delete at all. Authority may be inherited, but disappearance must still be recorded, and only the repository knows what a delete actually touched: the caller's loaded copy is depth- and policy-bounded, so auditing from it would silently under-report. |
| 2026-08-03 | The renderer acceptance matrix resolves its golden contract through each renderer's own entry point — `TideApp` and `RecordEditScreen`, `QtBrowseController`, the Web presentation manifest — and resolves evidence pointers by importing the named test rather than matching its source text. The checks themselves are exercised against deliberately broken input. Web evidence remains a source match, `python-gui` evidence skips where the optional extra is absent, and Qt navigation stays covered by the widget suite; each limit is recorded rather than implied. | The matrix read as an executable parity guarantee but could not fail for its stated purpose: it compared only the shared helpers against the Web projection, so TUI and Qt code was never executed, and evidence was a regex for a `def` line that stayed true for a module that no longer imported. Both gaps were live — three forked label helpers and two forked computed-field previews had already drifted underneath it. Comparing what each renderer actually resolves is what makes drift visible, and mutating the checks is what proves they still refuse. |
| 2026-08-03 | Naming and aligning a field is one implementation in `presentation.py`, and the Textual table helpers take the model's formats as a required argument rather than defaulting them. Inline collection columns resolve through `browse_columns` in every renderer. | Seven copies decided independently whether an explicit `label` wins, and they had already diverged: the Textual form humanized with `str.title()` over the raw name, so `customerName` read as "Customername" in the form and "Customer Name" in the grid one screen behind it. Alignment had diverged twice over — the report builder could not express an explicitly left-aligned number, and the terminal justified on field type alone and ignored `align` outright. A defaulted `formats` argument would have left that second mistake one forgotten call away, so it is required. Resolving inline columns through the shared helper also gives the terminal the unknown-column guard the other renderers already had, at screen construction rather than as a `KeyError` on mount. |
| 2026-08-03 | Renderers resolve `immutable_when`, `visible_when`, and `enabled_when` through `presentation.action_state` and `presentation.field_is_immutable`. An action is enabled only while it is visible, and a condition that cannot be evaluated hides the action or locks the field rather than raising. The service keeps its own check at commit and is unchanged. | Three copies decided this, and they disagreed. Only the REST projection survived a condition it could not evaluate; Qt and the terminal let the error escape into a form that was already open. The terminal also evaluated the two action conditions independently, so a posted invoice's hidden Post button still counted as enabled. Failing closed is right for a renderer because it is predicting what the service will allow: predicting "yes" offers a button the commit refuses, while predicting "no" withholds one it would have accepted. Nothing here authorizes anything, which is why the service still re-checks `enabled_when` when the action runs — presentation deciding conservatively cannot widen what is permitted. |
| 2026-08-03 | The Studio view preview resolves through `presentation.py` like a renderer, and reports a size warning only where the view declares one. `surfaces.tui` stays authored metadata that Studio alone reads. | The preview reused the editor's structure, which is source-shaped so a designer can move and remove what the document says. `sales.Invoice.edit` sets `compact_groups`, so it authors three scalar groups and every renderer draws one merged header — the preview showed three. A preview that shows a layout no renderer produces is worse than none, because it is believed. The sizing story failed the same way from the other end: Studio fell back to 80 or 60 columns, 14 per unconfigured column, and a summed row estimate, then reported them as a declared minimum. The terminal auto-sizes an unconfigured column and never refuses to draw at any size, so there was nothing for those numbers to be an estimate of. Warning only on declared values keeps the feature for authors who want a floor without inventing one for everyone else. |
| 2026-08-03 | Startup cross-checks every `execute:` and `generated_by:` reference against what the application's runtime hook registered, and refuses to start when one is missing. A registration nobody references is named alongside a missing one but does not by itself stop startup. | The compiler proves the referenced function exists in the project source; nothing checked that the hook registered it under the name the metadata uses, and the two are independent because `execute:` is a string key looked up in a dictionary. A missing or mistyped registration therefore surfaced as a `RuntimeError` the first time somebody ran the action — a deployment that starts cleanly and breaks on use. A typo produces both a missing reference and an unused registration, so naming the near miss points at the line to fix; failing on an unused one alone would punish a hook shared across model variants for something that costs nothing at runtime. |
| 2026-08-03 | `execute:` and `generated_by:` name a **domain** function, whose signature deliberately differs from the runtime handler signature; `runtime.py` is the adapter that bridges them. The compiler therefore validates the reference by name only, and handlers are not auto-registered from metadata. | The alternative was to require the referenced function to take `(values, context, repository)` directly, which would let the compiler check arity and let the runtime register handlers itself. It would also push transport-shaped parameters into functions whose whole point is to be ordinary domain code — `allocate_invoice_number(sequence, invoice_date)` should not learn what a `Repository` is. The lambda in `runtime.py` is not a workaround for a missing feature; it is where that adaptation belongs, and keeping it means the compiler is checking the right thing when it checks only the name. What was genuinely missing was the startup cross-check, which is now in place. |
| 2026-08-03 | Naming one record of an entity is one implementation in `presentation.record_label`, declarable per entity as `record_label:`. The generic web collection widget takes its row wording from the manifest's `record_label` and the field it numbers rows by from `sequence_field`, which the server derives from the collection's `order_by`. | Eight layers stripped a trailing "s" to guess a singular: the REST projection, Qt, three Textual modules, and the web client, each an independent guess at English pluralisation. It reads "Addres" for an entity labelled "Address", "Entrie" for "Entries", and leaves any non-English label plural — so the guess needed both one home and a way for a model to override it. The web client's leakage was the same problem from the other end: it hardcoded `line_number`, one sample application's field name, in the widget every application shares, and told every user their calculated values are finalized "when the invoice is saved". The collection already declared its ordering in `order_by`, so the manifest could always have said it. A collection is named after what its own form calls it rather than after the target entity, because a section headed **Lines** offering an **Add Invoice Line** contradicts the heading directly above it. |
| 2026-08-04 | A field's pattern contract is `edit_mask: {regex: ...}` and nothing else. The `validation: <id>` key is removed from the model rather than finished, and the web client's `email` special case goes with it. | `validation:` was a second way to say what `edit_mask` already says, and it was never finished: no registry defined an id, `_validate_entity` never read the key, and the compiler never checked that it named anything — `metadata.get("validation")` appeared exactly once in the whole tree, to copy the id into the presentation manifest. The one field that used it, `crm.Customer.email`, was therefore checked only by the web client, which recognised the literal string `email` and applied a regular expression of its own; the terminal, Qt, MCP and REST all accepted anything. Finishing it would have meant inventing a rule library, a compiler check, and a second enforcement path for a feature one sample field used, all to reach where `edit_mask` already was. Removing it makes `extra="forbid"` report the dangling key for free, and turns a claim the tutorial already made — that the email rule is enforced below the renderer — from false into true. The cost is the client's `<input type="email">` hint and its nicer message; keying that off a declared format name would have relocated the same hardcoded-vocabulary coupling rather than removed it. |
| 2026-08-04 | The database's unique index is the guard for `unique: true`. The service asks one indexed existence question per unique field before writing, and translates the index's refusal into the same structured validation issue when it loses the race. A constraint the service does not model keeps its own error. | The pre-check used to hydrate every row of the entity, per unique field, on every commit — with child-collection loading on the way — and to do it on a separate connection before the write transaction opened. So it cost more the more rows existed, and it was a time-of-check/time-of-use race regardless: the index was already the only thing that actually guaranteed uniqueness, and the scan bought nothing but a nicer message. A bounded question buys the same message at a fixed cost. Translating the index's refusal is what makes losing the race indistinguishable from winning it — a raw `IntegrityError` reaching a caller is a 500 for what is an ordinary "that code is taken". The translation re-asks rather than parsing driver error text, because constraint names are dialect-specific and a foreign key reported as a duplicate would send someone looking for a conflict that is not there. |
| 2026-08-04 | A unique generated value is claimed from a named sequence (`tide_sequence`, one atomic increment-and-read), not derived from stored rows. Sequences are not gap-free, and the migration proposer leaves the `tide_` prefix alone. | `peek_next_identity` was `MAX(primary_key) + 1` on its own connection outside any transaction, and the invoicing application built its unique invoice number from it — so two concurrent creates read the same value and the second user got a failure instead of the next number. A sequence prevents the collision instead of recovering from it, and keeps generators where #33 put them: ordinary domain code that runs before persistence and never learns what a transaction is. Allocating inside the caller's write transaction would have closed the gap window too, but only by inverting that layering and serialising every create in the application behind one lock. A gap is a number nobody used; that is the cheaper defect. The proposer had no notion of framework-owned tables because this is the first one in the repository's own metadata — without the prefix rule it offered to drop TIDE's bookkeeping as undeclared. |
| 2026-08-04 | A CRUD audit event is written inside the record's own write transaction, through an `on_written` callback the repository invokes on its connection before committing. An audit write that fails rolls the change back. | The event was written after the data transaction had already committed, on its own connection, so a crash in the gap left a change nobody could account for — the one outcome an audit trail exists to prevent. Failing the change when it cannot be recorded is the right direction to fail: a refused write is visible and retryable, while an unaudited one is silent and permanent. The callback is the smaller of the two fixes the issue proposed; a full unit of work on the Repository protocol would also let one service operation span several writes, which is still open. The audit store enlists only in a connection from its own engine — they are the same in every managed deployment, and writing the trail into a different database than the record would be worse than the gap it closes. |
| 2026-08-04 | A throttled sign-in is refused before the password hash runs, and concurrent verifications are capped; exceeding the cap answers 503 rather than 401. | `login()` computed `throttled` and then hashed anyway, so every refused attempt still bought ~600,000 PBKDF2 iterations — roughly a third of a second of CPU per cheap request, in handlers that share the server's bounded threadpool. Throttling made the attack *cheaper* to mount than to defend. Short-circuiting reveals that a username is currently throttled, which whoever caused the throttling already knows; that is a far smaller problem than the amplification. The cap exists because per-username throttling cannot see an attacker who never repeats a name — no identity-keyed limit can. Answering 503 instead of 401 matters for the honest user: told their password was wrong, they would go and reset a password that is correct. The dummy-hash equalisation stays on the un-throttled path, which is why the short-circuit keys on throttling rather than on whether the user exists. |
| 2026-08-04 | A new record written under an identity another record already holds raises `DuplicateIdentityError` from both repositories, and the SQL adapter earns that classification by asking whether the identity exists rather than by reading the driver's message. It stays a server error. | The document-shaped repository reported the collision as `ConcurrencyError(None, None)` — a stale-version error, for something versions have nothing to do with, whose message read "expected None, current None". Stale version is an instruction to reload and retry, and a taken identity is taken however many times the write is retried, so the class sent clients down a path that could not resolve it. The SQL adapter disagreed anyway, reporting an unclassified constraint violation, which is the more interesting half: two adapters that answer the same question differently mean a test against one proves nothing about the other. Asking the store re-uses the technique #16 established for unique fields, for the same reason — constraint names are dialect-specific, and a foreign key relabelled as a duplicate identity would name the wrong cause. It could in principle miss a row deleted between the rollback and the question, in which case the write keeps the general error rather than claiming a specific one it cannot support. It is not demoted to a validation failure because no client can cause it: primary keys are system-owned and the service refuses a caller-supplied one before any repository sees it, so reaching it means identity allocation issued a number twice. |
| 2026-08-04 | The web journey tests paste the development token through one `connectWithToken` helper instead of typing it, and the suite's `testTimeout` is 15s rather than vitest's 5s default. | The heaviest test failed about a quarter of full-suite runs on `Test timed out in 5000ms`, and measurement found roughly a third of its runtime went to typing a thirty-nine character fixture token one character at a time, eight times across four files. `user.type` yields to the event loop after each character so the application can react, which re-renders the whole shell per letter. That yield is not waste and was not removed: dropping it (`delay: null`) cut the test in half but broke `clear`-then-`type` on a controlled input, which typed `EURUSD` into a field that should have read `USD` — the render between interactions is load-bearing. Nothing is testing what happens between the letters of a token, so the token is pasted and every real field interaction still types. Two other levers were measured and rejected at ~10% each: `pointerEventsCheck: Never` and `css: false` both buy their speed by making jsdom stop resolving styles, which is what lets a test notice it is clicking through an overlay. What remains is genuine work — a full connect-browse-edit-save-resolve journey through a real React tree — so the budget was set to match it rather than left at a unit-test default the suite was clearing by two hundred milliseconds. |
| 2026-08-04 | Displaying a stored value is `labels.value_label`, one implementation, and it stays separate from `humanize`. Identifier labelling in the Qt lookup placeholder and omitted-collection note moves to the shared `presentation.field_label`. | The expression `str(value).replace("_", " ").title()` was written out twelve times — eight for choice values across Qt, Textual and reporting, and four more for identifiers that the filed issue did not list. They agreed, which is precisely how the field-label transform looked before one copy met a camelCase field name; two of these twelve had already drifted that way, because Qt built its own labels with `.get("label", ...)` and no camel splitting, so `customerName` read "Customername" there and "Customer Name" in every other renderer. `.get("label", default)` also differs from `field_label`'s `or` on an empty declared label. Keeping value labelling separate from `humanize` is the issue's call and the right one — a choice literal is the application's data, not a name TIDE assigned — but the stated rationale does not fully survive contact with `title()`, which turns `inProgress` into "Inprogress" rather than preserving the author's casing. That is carried over unchanged rather than fixed: it alters what deployed applications display, which is a model author's decision. Whether a choice should carry a declared label at all is still open. |
| 2026-08-04 | A Textual message handler asks for its *panel* before touching the controls inside it, through a query that returns `None` rather than raising. The guard sits on the query, not on whether the app is running. | Third instance of the same pattern, after #4 and #5: a handler assuming the widgets it queries exist. `#view-field-group-choice` is composed statically and only ever hidden, so `NoMatches` could only mean the query ran while the tree was out of the DOM — before compose mounts it or after teardown removes it, and a queued `RowHighlighted` is delivered in both windows. Guarding the single reported call site would have moved the crash to the five other queries the same call chain makes two lines later; guarding the container answers for all of them at once, because they arrive and leave together. Guarding on "the app is no longer running", which the issue floated, covers only the teardown half and none of the other entry points. The property branch of the same handler had the identical exposure and was not reported — fixing one and not the other would have left the pattern in place, which is the thing worth removing rather than this particular traceback. |
| 2026-08-04 | `/docs`, `/redoc` and `/openapi.json` are served only when asked for. `build_fastapi_app` defaults `docs` off; `tide serve` turns it on for a loopback bind and off otherwise, with `--docs` / `--no-docs` to say so explicitly. | The document is not a courtesy — it is every exposed entity, field and action plus the `x-tide` runtime configuration, which is a map of the application and its data model handed to anyone who can reach the port, before authentication. It was pinned in place by a test asserting an anonymous 200, so the behaviour had been ratified rather than overlooked. Withheld rather than gated behind a bearer token: a credential is not what makes publishing the model surface acceptable, and Swagger UI is a browser page that would need credentials of its own to fetch a protected schema, which is a login flow for a debugging aid. The library default is off rather than loopback-aware because `build_fastapi_app` does not know what it is bound to — only the CLI does — and a caller that never considers the question should not be publishing one. Loopback keeps it on because that is where the tutorials use it, matching how development authentication is already scoped. |
| 2026-08-04 | `B023` joins the selected ruff rules. The 29 findings were restructured out rather than suppressed: the compiler's computed-cycle walker and one Textual size check became named functions, and the nested-input factory takes its owning entity as a parameter. `BLE001`, `TRY004` and `DTZ` were reviewed and not adopted. | A closure over a loop variable reads whichever value the loop reached last, and this runtime builds per-entity and per-field callables in loops, so a miscapture would bind the wrong entity and raise nothing. All 29 were in fact safe — every closure was called inside the iteration that made it and none was stored — but "safe because of how it happens to be called" is not a property anyone can see from the call site, and `noqa` on 29 lines would have made the rule mean nothing. With them gone the rule is free to mean what it says, which is the point of selecting rules explicitly rather than inheriting them. The other three groups were reviewed for the specific fears the issue named and none held: every blind `except` on the security path already fails closed and says so in a comment — the JWKS lookup returns `None` so authentication fails, the readiness probe answers 503, the action store re-raises what it captured — and no timestamp anywhere is naive, because `datetime.now()` and `utcnow()` appear nowhere in the tree and the action clock refuses a value without a timezone. The `DTZ` findings are all `date.today()` for business-date defaults and `strptime` on typed input, where local is arguably the right reading; changing it is a product decision, not a lint fix. |
| 2026-08-04 | The browser session store's own lock covers the mapping only. Every call that can reach the identity provider runs under a per-session lock, and the mapping is re-checked afterwards before the access is handed back. | `authenticate_session` held the store-wide lock across the token refresh, which is a blocking HTTP POST to the provider. Sessions live in one mapping, so every authenticated request in the process contends on that lock: one slow provider response serialized all of them, up to the client timeout, for users nowhere near needing a refresh. Throttling everyone because one session needs a new token is the opposite of what the lock is for. The refresh was not the only exposure — verifying an access token fetches signing keys on a JWKS cache miss, so that call was under the same lock and is now under the same per-session one. Re-checking the mapping after the provider answers is what keeps the finer locking from weakening sign-out: without it, a request already in flight would return a valid access for a session that was ended while the provider was thinking. Two readers of one session still refresh it once, because they serialize on that session's lock and the second finds the token already renewed. |
| 2026-08-04 | A local-password session is re-checked against the user store on an interval, 30 seconds by default. Deletion, disablement, a changed password and losing every permitted role end it; a changed role set reaches the principal without ending it. `revoke_user` and `revoke_all` end sessions immediately. The session identifier and CSRF token still do not rotate mid-session. | Sessions were checked once, at sign-in, then trusted for eight hours — so disabling an account, resetting its password or withdrawing its roles changed nothing until the browser was closed, while the OIDC path next to it re-validated on every request. Per-request parity was measured rather than assumed and rejected at 1.6ms a call: a fresh SQLite connection, a schema check and two queries, which caps this path near 600 requests a second. An interval bounds the exposure to something an operator can reason about, and the measurement is why the number is what it is. Ending the session on a *changed* role set would have logged people out for being granted something, so the current roles are carried onto the existing principal instead — the security-critical direction, withdrawal, still takes effect. The password digest is stamped at sign-in so a reset is recognisable as a different credential; that makes `tide auth set-password` the cross-process sign-out-everywhere, since CLI and server share the store file while `revoke_user` only reaches the memory of the process that calls it. Rotation is left alone deliberately: a fresh identifier and CSRF token are minted at every sign-in, which is where fixation is actually prevented, and rotating mid-session has to reach the response and the browser to mean anything. |
| 2026-08-04 | Transport and identity-store hardening: the identity file is restricted with `icacls` on Windows as well as `chmod` elsewhere, a successful sign-in re-hashes a password stored below the configured work factor, responses carry a same-origin content policy and HSTS over TLS only, a structured query returns only messages TIDE composed, and a reserved Windows device name is refused by validation. | Five small items with one thing in common: nothing failed loudly. The `chmod` was skipped on the documented primary platform, so the store was least protected exactly where it is most used — `icacls` is best effort, because a hardening step that refuses to start is an outage. Re-hashing on sign-in is the only moment the plaintext is both in hand and known good; it moves the hash without the password changing, so the sign-in re-stamps that user's live sessions, and the session it is about to create stamps the *upgraded* hash — a test caught that being the pre-upgrade one, which would have signed everyone out on the first login after a work-factor bump. The content policy said only what may frame the page and nothing about what may load in it. `_bad_request(str(error))` was returning `[<class 'decimal.ConversionSyntax'>]` to callers, but replacing every message would also have discarded "unknown query field 'nope'", which is true, safe and actionable — so `QueryFieldError` marks the messages TIDE composed and everything else becomes generic. `con` and `nul` failed at `os.rename` when a plan was applied rather than when it was validated: closed either way, but diagnosed from the wrong layer. |
| 2026-08-04 | `LocalUserStore` gains `set_enabled` and `set_roles`, with `tide auth disable-user`, `enable-user` and `set-roles`. Roles are replaced rather than merged, an account may not be left with none, and the command checks role names against the compiled model because the store cannot. | `enabled` was read and never written: `login` refused a disabled account and, after the session revalidation work, a live session ended at the next check — but nothing in TIDE could disable one. A guarantee with no supported way to trigger it is a strange thing to ship, and its tests had to reach for raw SQL to prove it. Roles were fixed at creation for the same reason. Replace rather than merge because an add-only interface can only ever grant, and withdrawal is the direction that matters. Refusing an empty role set keeps one idea expressed one way: an account nobody can sign in to is a disabled account, and saying it through an empty role list would be saying it unclearly. The role-name check lives in the command because the store deliberately knows nothing about the application model, and the disable message says sessions end at the next revalidation rather than immediately, because the command runs in its own process and cannot reach the server's memory. |
| 2026-08-04 | `reserve_sequence_value(name, value)` raises a sequence's floor and never lowers it, `sequence_name(entity, field)` derives the name, and a demo-data provider may declare `load_sequence_floors()`. Raising the floor stays the application's job. | A sequence started at 1 regardless of what was already stored, so adopting TIDE over historical invoices would reissue numbers customers had already been shown. The framework cannot do this automatically, which is the part worth recording: the stored value is whatever the application's generator rendered — `INV-{year}-{sequence:06d}` in the sample — and no framework can invert an arbitrary formatter to recover the integer behind it. So the filed suggestion of raising floors from `Repository.seed` for any `generated_by` field is not implementable, and the mechanism is offered to the only code that can use it. Raise-only because adoption code runs more than once — a re-import, a retried migration step — and a rewind reissues numbers, which is the defect itself. The name is derived as `<entity>.<field>` rather than declared, matching the convention the #15 scaffolding already emitted; a field wanting its own name would declare one and this becomes the default, the same shape as `record_label`, so choosing the convention now forecloses nothing. The demo's numbers did not collide before only because seeded values are four digits and generated ones six — luck, which is why the guard asserts the floor rather than comparing rendered numbers. |
| 2026-08-04 | A deletion's audit entries are written inside the delete's own transaction, through an `on_deleted` callback, and a cascade's removals are audited together. | The #18 work put CRUD audit inside the write and left delete exactly as it was: `records.delete` recorded its events after the repository had already committed, on separate connections, one transaction per removed row. So a crash in the gap left a record gone with nothing accounting for it — the same defect #18 fixed, in the place it costs most. A create that is not audited can still be inspected afterwards; a delete that is not audited leaves nothing to inspect. The cascade case was worse still, because a failure partway through left some removals recorded and others not, which reads as though the unrecorded rows were never removed at all. Passing every removal to one callback rather than auditing them one at a time is what makes that all-or-nothing. This is the concrete instance behind #46; the general unit of work spanning several service writes has no caller today and is not built on speculation. |
| 2026-08-05 | `useDialogFocus` holds keyboard focus inside a modal dialog, places it there when the dialog opens, and returns it to whatever opened it. All three web dialogs use it, and a test fails if a component declaring `role="dialog"` does not. | `aria-modal="true"` is a promise that the rest of the page is unreachable, and nothing enforces it. Three dialogs made it and one kept it — the lookup dialog, which held the only copy of the trap. In the other two, `Tab` from the last button walked into the form behind the backdrop, where a keyboard or screen-reader user operated controls they could not see. Restoring focus on close was missing everywhere, so each dialog ended with focus on the body and the next `Tab` starting from the top of the page. The element to restore to is read during the first render rather than from an effect, because React applies `autoFocus` while it commits: the conflict dialog autofocuses **Continue editing**, so an effect would record that button, and it is gone by the time focus needs to go back. Initial focus is placed only when the dialog has not already placed it — `autoFocus`, or the lookup's own aim at the search box, knows better than "the first control in the markup". The fourth copy is prevented by a guard on the shape rather than by review, the same treatment the twelve label transforms and the three Textual polling loops got; it checks that a dialog component mentions the helper, which catches the dialog written without one and not a wiring mistake inside a file that does. |
| 2026-08-05 | The web renderer's API base path is `VITE_TIDE_BASE_PATH`, defaulting to `/api/v1`, and a server-supplied path must sit under it rather than under a literal `/api/`. A `..` segment is refused. | `build_fastapi_app(base_path=...)` has always been a parameter, and the manifest builds every resource, query, selection, report and browser-authentication path from it — but the renderer had `/api/v1` written into it twice. Once as the path it asked for, so its first request missed; and once inside the guard on server-supplied paths, so even a correct manifest from a server hosted under `https://host/tide/` was refused as unsafe. Configuring the constant alone would have fixed neither half. Build time rather than runtime because the bundle is static and there is nowhere to read a runtime value from: `StaticFiles` serves `index.html` verbatim, so a server-injected value would mean a custom index handler and its own caching rules, and a config endpoint needs a path to fetch it from. One build per deployment is the bargain `VITE_TIDE_API_TARGET` already makes for the development proxy, which now follows the same variable so it forwards what the renderer asks for. Anchoring the guard on the configured base is narrower than `/api/`, not looser — the manifest names every path the browser will call with the session's credentials attached, which makes it the one place a confused or compromised server could aim them elsewhere. The `..` rejection closes a gap that was there before: `fetch` resolves the path before sending it, so `/api/v1/../admin` passed a prefix test and requested something else. Sub-path hosting still needs the server's `root_path` and Vite's `base` to agree; this is the renderer's half of it. |
| 2026-08-05 | The open view and the open record are query parameters — `?view=` and `?record=` — not path segments. The record is carried as text, a view named in a link is checked against the manifest, and changing the view drops the record in the same history entry. | Both lived in a `useState`, so the address bar never changed: there was nothing to send anyone, the back button left the application entirely, and a refresh returned to whichever view happened to be first in the manifest and no record at all. Query parameters rather than paths because the built client is served by `StaticFiles`, which returns `index.html` for the root and 404 for anything deeper — a path-shaped URL would deep-link correctly from a link and then fail on the first refresh, which is worse than not having it. Text rather than the typed identity because that is what a URL holds, and nothing needed the type: the record GET path, the report path and the match against a loaded row all already went through `String`. The address bar is a caller like any other, so a view it names is checked against the authenticated manifest — a link shared by someone with more permission gives the recipient their own default, not an empty shell. The record is not checked the same way, because the renderer cannot know what identities exist; a bad one surfaces as the detail's own load error. Dropping the record when the view changes is not tidiness: entities number their rows from one, so `record=1` carried from Invoices to Products opens a customer nobody asked for. It happens in the same history entry as the view change, because two `pushState` calls for one click make Back a half-step. Previous/Next between records each push an entry, so Back walks back through the records that were actually looked at, which is what an addressed record implies. |
| 2026-08-05 | A downloaded report is named `<report title>-<record display>`, or `<report title>-<generated date>` for a summary. Rendering an entity's `display` becomes `presentation.record_display`, beside `record_label`. | `suggested_filename` was `f"invoice-{record.get('number', primary_key)}"` in framework code: two of the sample application's words, so every application's record reports were named after an invoice and any entity without a `number` field fell back to its key. Both halves were already declared — the report's `title` is what the reader saw on screen, and the entity's `display` is how the application says a record names itself — so this is using metadata that existed rather than inventing any. The summary path was deriving its prefix from the report *name*, which is an internal identifier; one rule for both is worth the changed filename (`sales-summary-…` reads `posted-sales-summary-…`). Neither bug is visible from invoicing, whose report is titled "Invoice" and whose entity displays itself by `number` — both wrong things produce the right answer there, which is why the inspection fixture gained a record report to see them. `record_display` sits next to `record_label` because they answer neighbouring questions and were being confused: one names the type ("Invoice"), the other the record ("INV-2026-0001"). |
| 2026-08-05 | `tide db seed` takes `--count NAME=NUMBER`, repeatable, replacing `--customers`, `--products` and `--invoices`. The numbers those flags defaulted to move into `applications/invoicing/fake_data.py`. | Three of the sample application's collections, spelled out in the CLI of a framework that is supposed to know nothing about invoicing and useless to anyone counting inspections. The layer below was already generic — `seed_fake_data` passes a plain `Mapping[str, int]` straight to the application's own provider — and the 2026-07-16 decision had already put fake-data profiles in application-owned providers; only the flags had stayed behind. Nothing in the framework knows which names mean anything, so an unrecognised one is not an error at this layer; what is checked is the shape, a negative number, and a name given twice, because a second spelling silently overriding the first is how a typo becomes a database with the wrong number of rows. The defaults move because how many of a thing makes a useful development database is a question about the application. |
| 2026-08-05 | `ActionService.__init__` reads `records.audit_store` and never writes it. | It used to assign it, so constructing an action service retargeted the caller's audit sink, and a second one over the same records service moved where CRUD audit went while leaving the first believing otherwise. Nothing needed it: every composition site in the tree already passes the same store to both, which is what makes it wiring rather than a fixup, and the whole suite passes with the assignment gone. A constructor that silently reconfigures an object it was handed is the kind of coupling that is invisible until two of something exist. |
| 2026-08-05 | One `load_application_module(path)` imports every Python file an application owns — runtime hook, demo data, fake data, an application's own action module, and the module generated applications load — registered in `sys.modules` under a name derived from the path. | Five copies, not the three that were filed, each naming the module `f"..._{abs(hash(path.resolve()))}"` and leaving it unregistered. Three quiet defects in one line: `hash` on a string is salted per process, so the same file is a differently-named module on every run and anything reading `__name__` reads a number that never repeats; a module absent from `sys.modules` cannot be found by its own name, so `typing.get_type_hints` fails on anything it defines, which under `from __future__ import annotations` — every file in this tree — is every annotation; and loading twice executed twice, producing two class objects per `class` statement, so `isinstance` says no between values from the same source. Caching means registering before execution, so a module can find itself while importing, and removing it again on failure, because a half-executed module left behind is handed to the next caller as though it had loaded. The generated-application template shrinks to two lines, which is the point: the boilerplate was being copied into every application TIDE writes. |
| 2026-08-05 | The end-to-end journeys drive a real `tide serve --demo --auth local --web-root web/dist`, started by Playwright through `web/tests/e2e/tide-server.mjs`, which also creates the run's account. Six journeys cover sign-in, browse, nested detail, create/edit/reload, draft-and-post, report preview and export, and a stale-edit conflict. | What was there served `dist/` from a hand-rolled static file server and asserted the login screen rendered: one test, no API, and a green tick that meant the bundle parsed. It could not have caught anything, and it did not — the first journey written against a real server found a selection payload the server had been refusing since the field was added. The launcher does its own setup because Playwright starts a web server *before* `globalSetup` runs, so an account created there would arrive after the server had already opened the store; doing both in one script puts the credentials on disk before the port accepts a connection. Each run mints a password from `randomBytes` and writes it to a gitignored file, so nothing committed or left behind can sign in to anything. Retries are off: a retry runs against a server the first attempt already wrote to, which is a different test wearing the same name. Every journey that writes creates what it writes to, so order does not matter and a rerun is safe — the seeded records are only ever read. Each journey was verified by breaking what it claims to cover: dropping `format_options` from the manifest, revoking `sales.invoice.detail`, un-posting the post action, removing the concurrency token, un-filtering the report, renaming the export, and taking the record back out of the address bar. Four of those are caught by exactly one journey. |
| 2026-08-05 | The Qt renderer is one module per screen, as the TUI already is: `workers`, `columns`, `editors`, `table`, `report`, `detail`, `conflict`, `collection`, `lookup`, `form`, and `app` for the two windows. `tide/qt/__init__.py` names which module holds each widget and loads it on first use. | 3,667 lines in one file, and the entry point for every reader of the renderer. The move is verbatim — 191 definitions in, 191 out, the only difference being eighteen helpers that lost a leading underscore on becoming shared, which is what `tui/table.py` does with `table_cell`. Two import cycles are real rather than accidental and are treated differently: a collection editor only *annotates* its parent form, so that is a `TYPE_CHECKING` import, while a lookup genuinely constructs an editor — a lookup can create the record it is looking for, whose editor can open a lookup of its own — so that one import waits inside the method that uses it. The package `__getattr__` was already there to keep PySide6 optional; it now also records where each screen lives, so callers did not change and neither did a single test import. The gain that was not the point: `tide.qt.app` had `ignore_errors` in mypy, and the code that moved out of it did not, which surfaced 23 real type errors — a lambda on a worker thread re-reading a mutable attribute after the check that narrowed it, a `row` used for both an index and a row of cells in one function, four widget types sharing one variable. Fixing them retires `tide.qt.app` from the ignore list rather than adding ten new entries to it. |
| 2026-08-05 | `RecordDetail` keeps its state and its markup. The three presentational sub-components move to `record-detail-sections.tsx`, and ten pure helpers move to `lib/` — `conflictFieldLabel` and `conflictRecordValues` to `conflicts`, `formTabs`/`collectionDraftState`/`issueFieldErrors` to `form-draft`, `actionApiError` to `api`, and the two focus helpers to a new `form-focus`. | The component is 1,136 lines and the last name on the decomposition list, so this is again a decision about where the seam is rather than about the size. Counting which of its thirteen pieces of state each handler touches settles it: `hydrateConflictSnapshot` touches twelve, the action mutation eleven, the save mutation ten. A `useConflictReview` hook — the obvious extraction, since four state atoms are named for it — would have to take a dozen setters as arguments or own everything, because rebasing after a conflict rewrites the draft, the collection drafts, both error maps and the selected tab. This is one editing session and its state says so, which is where the Qt edit dialog and the browse controller also landed. What is separable is what holds no state: three sub-components that take props and return markup, and ten functions with no React in them at all. Moving the helpers turned up a byte-identical copy of `conflictFieldLabel` in `record-conflict-review.tsx` — the twelfth instance of the pattern #42 was filed for, found because the decomposition made it look at both files at once. Breaking the shared one now fails tests in both consumers, which it could not have done while each held its own. |
| 2026-08-05 | `QtBrowseController` keeps its 60-odd methods. Only the reference-display cache leaves, as `ReferenceDisplayCache` in `tide.qt.references`. | The controller is 1,677 lines and was on the decomposition list, so this is a decision not to split it, made from measurements rather than from its size. Grouping the methods by name gives nine plausible modules; counting the calls between those groups says only one of them is a cluster. `report` makes no calls at all in either direction and is 45 lines. `conflict` makes six calls out and none within itself — it is a thin layer over form, collection and format, so extracting it would add a hop and remove nothing. `capability` is thirteen mostly one-line readers of `_entity_capabilities`. `form`, `lookup` and `collection` are cohesive but every one of them needs `client`, `model`, `session`, `entity` and `detail_view` together, so they would become objects holding a reference to everything they left behind, which is the same coupling with more indirection — the conclusion the Qt edit dialog reached, arrived at the same way. Seven methods out of sixty-six touch nothing but immutable model and view state, and they are four to thirty lines each; there is no free-function layer waiting to be lifted. What is genuinely separable is the reference cache: a dict, a lock, four touch points, and the controller's only mutable state, with `reset_browse` existing solely to clear it. Moving it leaves every remaining controller attribute fixed once construction finishes, gives the double-checked `setdefault` pattern one home, and makes it testable — none of "the same record is fetched once", "two threads racing agree", or "a record you may not read reads as Protected" had a test while it lived inside the class. |
| 2026-08-05 | `tide.development.studio` becomes a package — `contracts`, `documents`, `properties`, `layout`, `preview`, `service` — layered so each module imports only from the ones below it. `studio_sequence` becomes a `TypeGuard`, and the five entity operations become one `StudioOperation` alias. | 2,608 lines holding three different things: the contracts callers receive, the service that builds them, and about fifty helpers doing the building. Thirty-six of those helpers cross a module boundary and lose their leading underscore, which is a higher proportion than the earlier splits and says something true — the marker was never meaningful inside one file, and after the split the package is the unit. The type checker had `ignore_errors` on the old module and not on the new ones, which surfaced fifteen errors falling into two shapes worth fixing rather than silencing. Twelve came from `studio_sequence`, a runtime "is this a list" check whose callers immediately read the value: as a `bool` it told mypy nothing, so every `document.get(...)` stayed `Any | None` through the branch that had just checked it; as a `TypeGuard[Sequence[Any]]` all twelve go away and the check now means what it always meant. The other three came from the five entity operations existing twice, as a `Literal` on `StudioPreviewAccess.operation` and as a bare tuple of strings in the service, so a `dict` keyed by one could not be passed where the other was expected; naming the type once fixes it and deletes the duplicate list. The last two were narrowing thrown away into a boolean — `is_collection = isinstance(name, str) and ...` loses the type that the branches then need — so the variable holds the narrowed name instead of a flag. `StudioGroupKind` was copied into all six modules by the header-preserving slice, the same way `SCHEMA_TYPES` was in the CLI; that is now a known cost of this kind of extraction rather than a surprise. |
| 2026-08-05 | `tide.cli` becomes a package with one module per command family — `run`, `serve`, `database`, `application`, `model`, `api`, `auth`, `mcp` — each declaring its own arguments beside the handlers that read them. `main.py` keeps `main` and a `_create_parser` that calls each family's `add_*_commands(commands)`. | 3,035 lines, of which `_create_parser` was 985: every command's arguments sat about a thousand lines from the handler reading them, which is the actual cost, not the file size. The families turned out to be genuinely separable — each block touched only the top-level subparsers object and its own locals — so the parser was cut where it already divided. Four helpers cross a family boundary and become public in leaf modules: `print_json`, and `RunStorage`/`open_run_storage` for opening whatever data source a command was pointed at, which `run`, `db` and `serve` all need. `parse_oidc_role_map` lives with `auth` and is imported by `serve`, because that is where the provider configuration belongs. One thing the split exposed rather than caused: `SCHEMA_TYPES` and its eight imports were copied into all eleven modules by a header-preserving extraction and only `model` uses it — a reminder that a verbatim slice moves module-level state as well as definitions, and that ruff will strip the orphaned imports but not the constant that keeps them alive. Verified by removing each `add_*_commands` call in turn: all ten make a test file fail, so no family is registered without something depending on it. |
| 2026-08-05 | `tide.tui.studio` becomes a package — `save`, `groups`, `layout`, `preview`, `app` — rather than a 2,548-line module. Its `__init__` re-exports the screens, so `from tide.tui.studio import StudioLayoutScreen` still works. | The same treatment `qt/app.py` got, in the package that already demonstrated the pattern. Every helper turned out to be used by exactly one screen, so nothing had to become shared and nothing was renamed. A package rather than `studio_app`/`studio_layout` modules because callers already import screens from `tide.tui.studio` and an internal rearrangement is none of their business. Two things the split turned up. First, slicing a module on its `class` lines leaves each decorator attached to the block above it — `@dataclass(frozen=True)` silently landed on a Textual `ModalScreen`, which is valid Python and would not have failed any type or lint check; definitions own the decorators directly above them, and the extraction has to say so. Second, `tide.tui.studio` had `ignore_errors` in mypy and the submodules did not, which surfaced five errors and retires the entry: a parameter typed `str | None` that no caller can pass `None` to, two callers reading `TreeNode.data` without the check its optional type asks for, a `preferred_label` holding two types, and `action_quit` overriding an `async def` with a plain method. That last one worked only because Textual awaits whatever an action returns, and nothing in the suite had ever pressed `q` — so both the override and the guard protecting an open YAML edit had no coverage at all. They do now, and the first version of that test proved nothing: while the edit is open the `TextArea` has focus and types the `q` rather than triggering the binding, so the guard has to be called rather than pressed, and `exit()` does not stop the app within one pause, so the assertion has to be on what the guard said rather than on `is_running`. |
| 2026-08-05 | `BrowserSessionAccess` lives in `tide.api.browser_session`, which imports nothing outside the standard library and `tide.runtime`. `browser_auth` re-exports it; `local_auth` takes it from the new module. | The `api` extra could not run `tide serve`, in any authentication mode, on a clean install — which is exactly what `docs/WEB-UI.md` tells people to do. `server` imports `local_auth` at module level, `local_auth` imported `browser_auth` for this one two-field dataclass, and `browser_auth` imports `httpx`, which is in `client`, `auth` and `gui` but not `api`. So framework-owned password sign-in, which makes no outbound HTTP call at all, needed an HTTP client, and every developer who followed the documented command hit `ModuleNotFoundError: httpx` raised three imports deep. It surfaced from CI, where a job installing precisely the documented extras could not start the server the journeys needed; a fuller local environment had hidden it. `local_auth` now imports with no extras installed at all, which is the honest boundary for a component whose job is a SQLite file and a PBKDF2 hash. The `tide auth` handlers keep their present shape: each imports `LocalAuthenticationError` inside the same `try` whose `except` names it, so an import failure there would raise `UnboundLocalError` instead of anything readable — but with the coupling gone that import cannot fail for a missing extra, and error handling no test can reach is not worth adding. |
| 2026-08-05 | A reference selection sends its draft in the wire types the server decodes, through the same `payloadValue` conversion a save uses. `referenceSelectionDraft` moves to `form-draft.ts` beside `mutationPayload`. | `decode_wire_value` takes an integer only as a JSON number and a boolean only as a JSON boolean, and refuses the whole request otherwise — a deliberate contract, and the save path already met it. The selection path handed over `draft[field.name]` untouched, which is whatever the text input last held. So choosing a Product on an invoice line sent `line_number: "1"`, got a 400, and the `on_select` assignments that are the entire reason for the round trip never arrived: no description, no unit price, and "reference-selection payload is invalid" under the lookup. It survived because the two paths diverge exactly where nothing looked: on the Invoice form every writable field is a string or a date, so the same bug is latent there and only bites on the line. The jsdom suite could not see it either — its stub echoed back whatever it was handed, and its assertion had `line_number: "1"` written into it as though the string were correct. That stub now refuses a payload the real server would refuse, so the fast gate can catch the next one. |
| 2026-08-06 | A page resolves how its references name themselves once, server-side, and carries the answers with it: `RecordsService.reference_displays` over a new `Repository.get_many`, surfaced as `QueryPage.references` and as `_tide.references` on the wire. Every renderer reads it instead of fetching. | The browse grid showed `customer` as "ADRIA - Adria Consulting" and bought that name one referenced record at a time -- the web client one authorized round trip per distinct visible value renewed as you scroll, Qt the same through its HTTP client, Textual one `records.get` each behind a process cache. Filed as a web defect; it is renderer-neutral, so fixing it in the browser would have left the identical cost in the other two. Measured against the real stack, a page of eight seeded invoices over three customers went from three `GET /api/v1/customers/N` to none, and the server pays one `SELECT ... IN (...)` per distinct target entity instead. The resolver carries the authority the per-record fetch had, because that fetch was the thing enforcing it: the target entity must be readable, its read policy still decides row by row, and it refuses outright to resolve a display over a field the reader may not see -- otherwise this becomes the way a guarded value reaches a grid, and the batch load applies no field policy of its own. It also refuses a display over a virtual computed field, which a stored-scalar load cannot compute and would silently render blank. Two things fell out. The child's pointer back at its parent is deliberately not resolved: the adapters disagree about whether it is even populated on a hydrated child, so resolving it would have put an adapter's habit on the wire. And a resolved name belongs to the value it named, so `collectionDraftRows` drops it while keeping the rest of the envelope -- `protected_fields` describes the field and survives editing, a name does not. `record_display` moved to `tide.display`, below both `tide.data` and `tide.services`, because importing `tide.presentation` from a service runs the adapters and comes back round; the API's third copy of the display-template parser went with it. |
| 2026-08-06 | The report service reads the names the page resolved instead of fetching each referenced record itself. | Found while checking who pays for the resolution above, and it turned out to be the worst instance of the same defect rather than a caller that wanted nothing. Every other renderer at least remembered a resolved name behind a cache; `_format_field` asked again for every cell it drew. `sales.summary` groups by `customer` with a 500-row limit, so a full report made up to five hundred `records.get` calls for a handful of distinct customers, and a record report re-fetched the same product once per invoice line. It now costs one resolution per document -- and the summary path pays nothing extra at all, because `QueryPage` already carries what its own query resolved. That is the argument for having put this in the service rather than in the wire projection: a consumer that never speaks HTTP gets the fix by reading a field. |
| 2026-08-06 | `Repository.transaction()` yields a repository of its own rather than taking a `unit=` on every method. Calls on the yielded object join the scope; calls on the original are refused while it is open. | Built at the owner's direction after I argued for leaving #46 as a design note; nothing in the tree writes two records in one operation yet, and that is still true. The shape is the part worth recording. A `unit=` keyword -- what the issue sketched -- can be forgotten at one call site out of ten, and the result is a write that quietly commits on its own inside what the reader takes to be a transaction; an object cannot be forgotten, because there is nothing else to call. It is a shallow copy of the repository with the scope bound, so it satisfies the same protocol and anything already written against `Repository` works inside a scope unchanged. Four rules, both adapters, twenty-four conformance tests: a clean exit commits and an exception rolls back; a failed *write* dooms the scope, because there are no savepoints and how much of a refused statement survives is the backend's business, while a failed *read* changes nothing and leaves a handleable `NotFoundError` handleable; a nested `transaction()` joins rather than nesting, so an operation built from two smaller ones is one commit; and a unit used after its scope ends raises. Writing the tests found two divergences that would have met the first real caller. A write that goes *round* the scope had three different meanings -- the document store undid it with the scope, a database would commit it independently, and SQLite in memory handed back the same pooled connection so it joined -- so it is refused rather than defined. And `write`'s `DuplicateIdentityError` probe assumes a rolled-back transaction and a fresh connection; inside a scope neither holds, and on a backend that aborts a transaction after a failed statement the probe would raise over the error it was sent to explain, so inside a scope it keeps the general error. |
| 2026-08-06 | A password work-factor upgrade is a compare-and-swap against the hash that sign-in verified; losing the race requires re-reading and verifying the current account before a session may be issued. | The previous update matched only the username. A concurrent `set-password` could commit after the old password was verified and then be overwritten by the opportunistic upgrade, making the administrator's new password invalid and the old one valid again. A conditional update gives reset priority. Re-reading also lets two legitimate simultaneous upgrades converge without spuriously refusing the one that arrived second. |
| 2026-08-06 | Local-session revalidation retries when another request replaces the same live session, while a missing mapping still means revocation. | Two requests can cross the thirty-second boundary together and both read the same immutable session object. The first replaced it with its refreshed copy; the second compared by identity, mistook that replacement for deletion and returned 401. Re-reading the mapping distinguishes concurrent progress from `revoke_user`, `revoke_all`, expiry or a failed account check without holding the process-wide session lock across SQLite I/O. |
| 2026-08-06 | SQL repository scope ownership is context-local rather than one mutable thread identifier on the shared repository. | A FastAPI repository is shared. Two threads opening scopes overwrote `_open_on_thread`, so only the last thread was protected from going around its yielded unit and the other could commit independently. A `ContextVar` separates concurrent threads and asynchronous execution contexts, is shared by the shallow unit copy, and retains the existing nested-scope and closed-unit semantics. |
| 2026-08-06 | Windows identity-store ACLs grant the SID reported by the current process token, verify that the file remains openable, and restore inheritance after a failed attempt. | `%USERNAME%` is an environment hint, not an authority boundary: sandboxed, serviced or impersonated execution can run as a different account. Dropping inheritance and granting the hint locked TIDE out of the SQLite file it had just created. A numeric SID also avoids localized or domain-qualified account-name ambiguity, while reopen/recovery preserves the documented best-effort availability contract. |
| 2026-08-06 | `tide db seed` requires an explicit application role; the framework does not choose one. | The second application exposed the remaining Invoicing leak after named seed counts became generic: the CLI still defaulted to `sales_clerk`. Inferring the first writable role would be ambiguous and could over-grant a provider, while a framework-owned role name would merely move the coupling. An explicit role is visible, reviewable, and fail-closed; each application shortcut documents its own choice. |
| 2026-08-09 | Local identity stores are ignored by pattern (`.tide/*auth*.sqlite3*`) rather than by a per-file list, and a test asserts the pattern already covers an application nobody has written yet. | Restricting the store's ACL is pointless if the file is then published, and this repository is public. The list was per-file until the Contacts shortcut created `.tide/contacts-local-auth.sqlite3`, which was not on it: `.tide/` turned into untracked content one `git add -A` away from committing an administrator's PBKDF2 hash. A per-file list makes correctness depend on remembering, which is exactly what the second application proved does not happen. The test compares the set of paths `git check-ignore` prints rather than its exit status, because that exits 0 when any one argument matches and would pass while five of six leaked. |
| 2026-08-09 | Gate commands name directories, not files: CI runs `ruff check src tests applications`. | The list read `applications/invoicing/actions.py`, so the second application arrived with four unlinted Python files and nothing said so. A hand-maintained path list silently narrows every time the tree grows; a directory only narrows when someone means it to. Verified by planting an unused import in `applications/contacts/demo_data.py`: the old command exits 0, the new one exits 1. `tide model validate` now also runs for both applications, so the command the documentation tells readers to run is the command CI runs. |
| 2026-08-09 | Developer shortcuts take the application as an argument rather than existing once per application: `start.bat <command> [application]`, and one `npm run dev:app -- --app <name>`. | The same list-shaped problem as the gate commands, one layer out. Contacts arrived as seven copied batch blocks and a fourth copy of the `tide serve` line in `package.json`, and a copied command line drifts -- `start.bat seed` passed `--customers` for weeks after the CLI replaced it, which only surfaced when a second application made the duplication visible. Now each command is written once and reads its application's label, roles, identity store and extras from a settings block; adding an application is that block and nothing else, and the launcher derives an unconfigured application's store from its name. The documented `contacts-*` spellings are kept as aliases so nothing already written stops working. |
| 2026-08-09 | The Windows shortcut is tested by running it under cmd.exe against stubbed `uv`, `npm` and `powershell`, not by matching substrings. | The substring assertions were a second copy of the script and agreed with whatever it said, including the retired `--customers`. Eighteen invocations now assert the exact command line each documented name produces, for both applications. Two things the harness has to know: cmd.exe *chains* to a batch file invoked without `call`, so a stubbed `uv` is the last thing that runs and these cases assert the command rather than what follows it -- the real `uv` is an executable, while `npm` genuinely is `npm.cmd`, which is why only that one is called with `call`. |
| 2026-08-09 | Coverage data fragments are ignored as `.coverage.*` beside the exact `.coverage`, and deliberately not as the `.coverage*` glob. | Making CI parallel changed what a run leaves behind, and the ignore rules did not follow. `pytest -n 4 --cov` writes one data file per worker named `.coverage.<host>.<pid>.<random>`, combined and deleted only when the run completes; a killed or interrupted one leaves them untracked in a public repository. The rule was the exact name `.coverage`, which matches none of them — the same per-file-list shape as the identity stores, arriving in the same commit that added the workers, and missed by the person who had just written that pattern down. The narrower rule is deliberate: `.coverage*` would also hide a `.coveragerc` added later, and the test pins both directions. |
| 2026-08-09 | A documented command a reader is invited to run lives in a `bash`/`powershell` fence and is checked against the parser; one TIDE does not have yet lives in a `text` fence. | The 106 runnable invocations across the documentation are the largest surface that spells out a `tide` command, and the only one whose runtime is a person — who gets an argparse error rather than a failing build. `tide api describe`, under "Useful commands" in `API-AND-MCP.md`, had never existed in any commit. The other four hits were honest: ROADMAP sketches "the shape the CLI is growing towards" and APPLICATION-MODEL a migration workflow the prose says does not exist. Prose is the wrong place to say that to someone copying a line, so those blocks became `text`, which this repository already used for illustrations. The check reads the subcommand off the line, so it is stricter than the launchers' union check, and it refuses an invented subcommand as well as an invented flag — the first version walked silently past `tide model export`. |
| 2026-08-09 | Every launcher's flags are checked against the CLI parser rather than against the string the launcher was written to produce, and the two Node launchers under `web/` were left separate. | Collapsing the per-application npm entries into `dev-app.mjs` created a *second* script in the same package that composes `tide serve`, beside Playwright's `tide-server.mjs` — the copy-shaped defect appearing in the act of removing one. Merging them fails the measure-before-splitting rule: about six flags are shared while user creation, the credentials file, `--web-root`, `--log-level` and stale-store cleanup are not. What actually breaks is a renamed flag, and neither a `--print` assertion nor CI would catch it in the dev runner — that one is executed by no job at all. `tests/test_launcher_contracts.py` introspects `_create_parser()` (milliseconds, and per-subcommand) and refuses any flag `tide` no longer declares, across both Node launchers and `start.bat`. Reverting `--count` to `--customers` now turns it red, which is the drift that went unnoticed for weeks. |
| 2026-08-09 | The renderer acceptance contract names the role it resolves as; the harness has no default. | `sales_clerk` was the fallback in `_contract_role` and two session helpers — an Invoicing role a second application does not define. Contacts only worked because its contract set one explicitly; a third that forgot would resolve as a role granting nothing and fail as an empty capability set, never as the missing setting. The role now lives in `docs/renderer-acceptance.yaml` beside the rest of the recorded contract, and a missing one is refused. |
| 2026-08-09 | The checked-in applications are compiled once per test session, keyed by path, with the source fingerprint re-checked once at the end rather than on every call. | The suite asked for `applications/invoicing` 408 times at about 76ms each. The obvious safe version -- fingerprint the directory on every call so an edit recompiles -- was correct and *28 seconds slower than no cache at all*, because an `rglob` and `stat` sweep on Windows costs more than the compile it protects. Checking once per application and again at `pytest_sessionfinish` pays that twice instead of 408 times, and turns the remaining risk into a loud failure: a path-keyed cache does serve a stale model after a mid-session edit, so the session is failed rather than reported green. Nothing writes to `applications/`, and that is now checked rather than assumed. Net 20s off a 193s serial run. |
| 2026-08-09 | CI runs pytest with `-n 4 --dist loadfile`; the worker count is pinned rather than `auto`. | 1.96x on the configuration CI actually runs (5:39.9 to 2:53 with coverage), same 978/969/9/0 in both, and no flakes across four repeat runs. Adopted rather than merely measured because three specific hazards were checked and absent: no test binds a real socket (`uvicorn.run` is monkeypatched, so the `:8443`-style literals are strings in assertions), there is no `cov-fail-under` for the one-statement coverage difference between serial and parallel to trip, and `loadfile` keeps each file on one worker so no suite loses its own ordering. `auto` is refused because a 16-core runner would start sixteen Textual pilots at once; they have real timeouts, and oversubscription makes them miss waits rather than fail honestly -- the same reason `web/vite.config.ts` caps its jsdom pool. |
| 2026-08-09 | `docs/examples/first-application` was deleted rather than re-documented once the tutorial moved to the generated Contacts application. | It was a second, differently shaped application also named "TIDE Contacts" -- one entity, and a `contact_manager` role granted nowhere else -- kept alive only by a test that compiled it. The prose referencing it went in the same rewrite, including a `Copy-Item docs/examples/first-application applications/contacts` step that would now overwrite the real application. Thirteen test modules already compile minimal hand-written projects into `tmp_path`, so it held a name collision and no coverage that is not held elsewhere. |

## Open decisions

- Compatibility policy and migration tooling for the eventual stable metadata 1.0 contract.
- Shared encrypted browser-session storage and key rotation, provider-wide
  logout/revocation behavior, and interactive desktop login for individual
  identity providers.
- Pagination, keep-together, group-band, image, and configurable page-geometry
  limits for the expanded report engine.
- Stable identifiers and explicit rename representation for schema evolution.
- Exact representation of protected fields in versioned REST contracts.
- Whether direct many-to-many syntax belongs in the first stable model.
- Composite-key representation and database-generated key/refresh behavior for
  broader legacy database compatibility.

## Deferred decisions

- General-purpose or arbitrary source-code generation beyond reviewed templates.
- Public plugin and application marketplaces.
- Alternative native runtime implementations in Go or Rust.
- General-purpose workflow language.
- Full browser delivery of Textual applications versus a dedicated web
  renderer.
